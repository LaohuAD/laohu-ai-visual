# 故事素材原子库实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. If the user explicitly authorizes subagent delegation, superpowers:subagent-driven-development may be used instead. Creating and validating the new Skill also requires `skill-creator`; code changes follow `test-driven-development` and completion follows `verification-before-completion`.

**Goal:** 建立一个只在明确保存意图下收录、能够完整保留原话并拆成故事原子、由编剧渐进检索且把未批准网络候选隔离在正式库外的长期素材系统。

**Architecture:** 新增独立负责人 `laohu-story-material`，以本地私有 `00_输入原料/故事素材原子库.jsonl` 为唯一数据真源，使用一个 Python 工具完成结构验证、原子写入、压缩搜索、按 ID 详情读取、使用事件和统计。总控负责触发路由，素材 Skill 负责收录与检索，编剧负责适配和创作，复盘负责把真实结果交回 usage 事件；外部搜索只形成待确认报告。

**Tech Stack:** Python 3 标准库、JSONL、`unittest`、Markdown Skill、YAML UI 元数据、现有能力场景验证器与 Bash 总检查。

**权威设计：** `04_诊断与系统日志/2026-08-29_故事素材原子库与编剧渐进检索设计.md`

---

## 文件结构与职责

### 新建

- `skills/laohu-story-material/SKILL.md`：自动发现、触发边界、收录与检索主流程、编剧交接、外部确认门。
- `skills/laohu-story-material/agents/openai.yaml`：UI 名称、说明和默认调用语句。
- `skills/laohu-story-material/references/01_原子记录与渐进检索合同.md`：三类记录字段、四层加载、查询充分度、外部候选报告和错误分支。
- `skills/laohu-story-material/scripts/story_material_db.py`：唯一确定性数据库工具。
- `tests/test_story_material_db.py`：数据库结构、隐私投影、状态事件、原子写入和失败保真测试。
- `tests/test_story_material_architecture.py`：独立负责人、触发路由、编剧强制检索、外部候选隔离和项目入口测试。

### 修改

- `.gitignore`：忽略真实素材库，不忽略 Skill、脚本和匿名测试。
- `AGENTS.md`：十三改十四，登记新负责人和路由，但不复制素材专业细则。
- `skills/laohu-ai-visual/SKILL.md`：识别明确保存意图；分享、讨论和当前剧本请求不自动长期保存。
- `skills/laohu-script-writer/SKILL.md`：正式创作前生成查询合同、调用渐进检索、允许零采用、记录素材交接和记忆点职责。
- `skills/laohu-generation-review/SKILL.md`：素材选择确为首断点时，把证据交回素材 Skill 追加 usage，不直接改库。
- `02_共享资产库/00_核心规则手册.md`：能力地图增加素材负责人，十三改十四。
- `02_共享资产库/05_工具流程/laohu_skills核心合约.md`：负责人、入口、交接、失败返回和 HTML 适用范围更新。
- `README.md`、`输入输出索引.md`：公开入口、用途和十四项能力更新。
- `skills/laohu-ai-visual/scripts/check_laohu_skills.sh`：登记第十四个 Skill 和数量断言。
- `tests/capability_scenarios.json`：增加素材 Skill 合同和收录、编剧、外部确认、复盘回流场景。
- `scripts/validate_capability_architecture.py`：增加真实素材库不得被 Git 跟踪的机械检查。
- `04_诊断与系统日志/能力进化台账.md`：新增 `EVO-20260829-003`，记录能力空位、保护项、验证和重开条件。

### 运行时私有文件

- `00_输入原料/故事素材原子库.jsonl`：首次真实执行“记录灵感”时由数据库工具创建；实施阶段不写入虚构素材占位，也不纳入 Git。

## 脏工作树保护

当前 `AGENTS.md`、能力地图、核心合约、三项既有 Skill、场景 JSON、验证器、README、索引和能力进化台账均有本轮开始前的未提交改动。执行时先保存 `git diff -- <计划文件>` 的基线，只在现有内容上做最小增量；不得 reset、checkout 或覆盖。新建文件可以单独提交，重叠脏文件不得建立声称只属于本功能的提交。

### Task 1：建立架构 RED 测试

**Files:**
- Create: `tests/test_story_material_architecture.py`

- [ ] **Step 1：写入独立负责人和跨 Skill 行为测试**

```python
from __future__ import annotations

import subprocess
import unittest
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]


class StoryMaterialArchitectureTests(unittest.TestCase):
    def read(self, relative: str) -> str:
        path = ROOT / relative
        self.assertTrue(path.is_file(), f"missing architecture file: {relative}")
        return path.read_text(encoding="utf-8")

    def test_story_material_is_an_independent_owner(self) -> None:
        skill = self.read("skills/laohu-story-material/SKILL.md")
        for anchor in (
            "素材箴言",
            "明确保存意图",
            "原话只保存一次",
            "source / atom / usage",
            "渐进检索",
            "允许零采用",
            "外部候选确认门",
        ):
            self.assertIn(anchor, skill)

    def test_router_saves_only_when_the_user_expresses_save_intent(self) -> None:
        router = self.read("skills/laohu-ai-visual/SKILL.md")
        self.assertIn("记录灵感", router)
        self.assertIn("laohu-story-material", router)
        self.assertIn("不擅自长期保存", router)

    def test_script_writer_must_query_but_may_adopt_nothing(self) -> None:
        writer = self.read("skills/laohu-script-writer/SKILL.md")
        for anchor in (
            "素材查询合同",
            "严格匹配",
            "放宽表面条件",
            "跨领域类比",
            "允许零采用",
            "素材调用回执",
            "记忆点账本",
        ):
            self.assertIn(anchor, writer)

    def test_external_candidates_cannot_cross_the_confirmation_gate(self) -> None:
        material = self.read("skills/laohu-story-material/SKILL.md")
        self.assertIn("external_candidate / not_approved", material)
        self.assertIn("未经老胡确认不得进入剧本或长期素材库", material)

    def test_review_returns_material_evidence_to_the_material_owner(self) -> None:
        review = self.read("skills/laohu-generation-review/SKILL.md")
        self.assertIn("素材选择", review)
        self.assertIn("laohu-story-material", review)
        self.assertIn("usage", review)

    def test_private_database_is_ignored_and_untracked(self) -> None:
        ignore = self.read(".gitignore")
        self.assertIn("/00_输入原料/故事素材原子库.jsonl", ignore)
        tracked = subprocess.run(
            ["git", "ls-files", "--error-unmatch", "00_输入原料/故事素材原子库.jsonl"],
            cwd=ROOT,
            text=True,
            capture_output=True,
            check=False,
        )
        self.assertNotEqual(0, tracked.returncode)

    def test_project_documents_register_fourteen_owners(self) -> None:
        for relative in (
            "AGENTS.md",
            "README.md",
            "输入输出索引.md",
            "02_共享资产库/00_核心规则手册.md",
            "02_共享资产库/05_工具流程/laohu_skills核心合约.md",
        ):
            text = self.read(relative)
            self.assertIn("laohu-story-material", text, relative)
            self.assertIn("十四", text, relative)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2：运行架构测试并确认 RED**

Run: `python3 -m unittest tests.test_story_material_architecture -v`

Expected: FAIL；首个失败为 `skills/laohu-story-material/SKILL.md` 不存在，其他失败指出路由、编剧、复盘、忽略和十四项能力尚未接通。

- [ ] **Step 3：确认原有单元测试仍是基线通过**

Run: `python3 -m unittest discover -s tests -p 'test_*.py'`

Expected: 新增测试失败；除 `test_story_material_architecture` 外，原有 21 项测试保持通过。

### Task 2：建立数据库 RED 测试

**Files:**
- Create: `tests/test_story_material_db.py`
- Create: `skills/laohu-story-material/scripts/story_material_db.py`

- [ ] **Step 1：写入数据库行为测试**

测试通过 `importlib` 加载脚本，避免引入 Python 包结构：

```python
from __future__ import annotations

import importlib.util
import json
import tempfile
import unittest
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
SCRIPT = ROOT / "skills/laohu-story-material/scripts/story_material_db.py"


def load_store():
    spec = importlib.util.spec_from_file_location("story_material_db", SCRIPT)
    if spec is None or spec.loader is None:
        raise RuntimeError("cannot load story material database module")
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    return module


class StoryMaterialDatabaseTests(unittest.TestCase):
    def setUp(self) -> None:
        self.tempdir = tempfile.TemporaryDirectory()
        self.db = Path(self.tempdir.name) / "materials.jsonl"

    def tearDown(self) -> None:
        self.tempdir.cleanup()

    def source_payload(self, original: str = "父亲没说关心，只把坏掉的插座换了。") -> dict:
        return {
            "original": original,
            "source_type": "life_observation",
            "source_url": "",
            "source_title": "",
            "fact_status": "personal_observation",
            "curation_priority": "laohu_selected",
            "privacy": "private",
            "rights_note": "",
            "status": "active",
        }

    def atom_payload(self, source_id: str, mechanism: str = "用解决实际问题代替直接表达关心") -> dict:
        return {
            "source_id": source_id,
            "atom": "人物用修理物件代替说出关心",
            "human_truth": ["嘴硬心软"],
            "mechanisms": [mechanism],
            "material_types": ["relationship_detail", "character_action"],
            "dramatic_functions": ["character_setup"],
            "memory_carriers": ["action", "object"],
            "relationships": ["父子"],
            "emotions": ["克制", "温暖"],
            "themes": ["家庭"],
            "visible_evidence": ["换掉坏插座"],
            "extensions": ["改为修好对方每天使用却没开口请求的物件"],
            "non_replaceable": "没有说关心，先修好了插座",
            "boundaries": ["不照搬真实人物身份"],
            "fit": {"formats": ["短片"], "tones": ["现实"], "roles": ["texture"]},
            "fact_confidence": "medium",
            "analysis_confidence": "high",
            "status": "callable",
        }

    def test_source_is_saved_once_and_long_original_is_not_truncated(self) -> None:
        store = load_store()
        original = "原话" * 5000
        source = store.add_source(self.db, self.source_payload(original))
        atoms = store.add_atoms(
            self.db,
            [self.atom_payload(source["id"]), self.atom_payload(source["id"], "用劳动掩饰歉意")],
        )
        records = store.load_records(self.db)
        sources = [record for record in records if record["record_type"] == "source"]
        self.assertEqual(1, len(sources))
        self.assertEqual(original, sources[0]["original"])
        self.assertEqual({source["id"]}, {atom["source_id"] for atom in atoms})

    def test_search_returns_compact_index_without_original(self) -> None:
        store = load_store()
        source = store.add_source(self.db, self.source_payload("不能出现在索引里的完整原话"))
        atom = store.add_atoms(self.db, [self.atom_payload(source["id"])])[0]
        result = store.search_records(
            self.db,
            {"prefer": {"mechanisms": ["用解决实际问题代替直接表达关心"]}, "limit": 10},
        )
        encoded = json.dumps(result, ensure_ascii=False)
        self.assertIn(atom["id"], encoded)
        self.assertNotIn("original", encoded)
        self.assertNotIn("不能出现在索引里的完整原话", encoded)
        self.assertNotIn("visible_evidence", encoded)
        overview = json.dumps(store.stats(self.db), ensure_ascii=False)
        self.assertNotIn("不能出现在索引里的完整原话", overview)
        self.assertIn(atom["id"], overview)

    def test_get_loads_only_requested_atom_source_and_usage(self) -> None:
        store = load_store()
        first = store.add_source(self.db, self.source_payload("第一条原话"))
        second = store.add_source(self.db, self.source_payload("第二条原话"))
        first_atom = store.add_atoms(self.db, [self.atom_payload(first["id"])])[0]
        second_atom = store.add_atoms(self.db, [self.atom_payload(second["id"], "用玩笑化解羞耻")])[0]
        store.log_usage(
            self.db,
            {
                "atom_ids": [first_atom["id"]],
                "event_type": "shortlisted",
                "scope": "project",
                "project": "测试作品",
                "script_position": "第一场",
                "usage_role": "texture",
                "transformation": "保留行动替代表达",
                "result": "pending",
                "evidence_path": "",
            },
        )
        detail = store.get_records(self.db, [first_atom["id"]])
        encoded = json.dumps(detail, ensure_ascii=False)
        self.assertIn("第一条原话", encoded)
        self.assertIn("shortlisted", encoded)
        self.assertNotIn(second_atom["id"], encoded)
        self.assertNotIn("第二条原话", encoded)

    def test_project_rejection_does_not_pause_library_but_library_event_does(self) -> None:
        store = load_store()
        source = store.add_source(self.db, self.source_payload())
        atom = store.add_atoms(self.db, [self.atom_payload(source["id"])])[0]
        common = {
            "atom_ids": [atom["id"]],
            "project": "测试作品",
            "script_position": "第二场",
            "usage_role": "texture",
            "transformation": "未采用",
            "result": "not_fit",
            "evidence_path": "",
        }
        store.log_usage(self.db, {**common, "scope": "project", "event_type": "rejected"})
        self.assertEqual("callable", store.search_records(self.db, {"limit": 10})[0]["effective_status"])
        store.log_usage(self.db, {**common, "scope": "library", "event_type": "paused"})
        self.assertEqual([], store.search_records(self.db, {"limit": 10}))
        store.log_usage(self.db, {**common, "scope": "library", "event_type": "reopened"})
        self.assertEqual("callable", store.search_records(self.db, {"limit": 10})[0]["effective_status"])

    def test_invalid_foreign_key_and_unapproved_external_leave_database_unchanged(self) -> None:
        store = load_store()
        source = store.add_source(self.db, self.source_payload())
        before = self.db.read_bytes()
        invalid_atom = self.atom_payload("SRC-20990101-999")
        with self.assertRaises(store.ValidationError):
            store.add_atoms(self.db, [invalid_atom])
        self.assertEqual(before, self.db.read_bytes())
        invalid_source = self.source_payload("网络候选")
        invalid_source["curation_priority"] = "ai_external_candidate"
        with self.assertRaises(store.ValidationError):
            store.add_source(self.db, invalid_source)
        self.assertEqual(before, self.db.read_bytes())
        self.assertEqual(source["id"], store.load_records(self.db)[0]["id"])

    def test_same_source_cannot_fill_the_whole_candidate_set(self) -> None:
        store = load_store()
        first = store.add_source(self.db, self.source_payload("同源案例"))
        second = store.add_source(self.db, self.source_payload("另一案例"))
        store.add_atoms(
            self.db,
            [self.atom_payload(first["id"], f"同源机制{i}") for i in range(4)],
        )
        store.add_atoms(self.db, [self.atom_payload(second["id"], "另一机制")])
        result = store.search_records(self.db, {"limit": 5, "per_source_limit": 2})
        self.assertLessEqual(2, sum(item["source_id"] == first["id"] for item in result))
        self.assertTrue(any(item["source_id"] == second["id"] for item in result))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2：运行数据库测试并确认 RED**

Run: `python3 -m unittest tests.test_story_material_db -v`

Expected: ERROR；`story_material_db.py` 不存在。

### Task 3：实现单文件 JSONL 数据库工具

**Files:**
- Create: `skills/laohu-story-material/scripts/story_material_db.py`
- Test: `tests/test_story_material_db.py`

- [ ] **Step 1：实现公共类型、校验、原子写入、搜索和 CLI**

`story_material_db.py` 使用以下完整标准库实现；它公开单元测试所需函数，并让严格、放宽和类比由 Skill 改变查询合同，而不是藏在脚本内部：

```python
#!/usr/bin/env python3
"""Private JSONL store for story sources, reusable atoms, and usage events."""

from __future__ import annotations

import argparse
import fcntl
import hashlib
import json
import os
import re
import sys
import tempfile
from collections import Counter
from contextlib import contextmanager
from datetime import datetime
from pathlib import Path
from typing import Iterator


ROOT = Path(__file__).resolve().parents[3]
DEFAULT_DB = ROOT / "00_输入原料" / "故事素材原子库.jsonl"
ID_PATTERN = re.compile(r"^(SRC|ATM|USE)-\d{8}-\d{3,}$")

SOURCE_TYPES = {
    "life_observation", "short_video", "conversation", "news",
    "personal_idea", "image", "audio", "other", "unknown",
}
FACT_STATUS = {"personal_observation", "sourced", "unverified", "fiction", "unknown"}
CURATION = {"laohu_selected", "laohu_approved_external"}
SOURCE_STATUS = {"active", "archived"}
ATOM_STATUS = {"callable", "pending_evidence"}
FACT_CONFIDENCE = {"high", "medium", "low", "unknown"}
ANALYSIS_CONFIDENCE = {"high", "medium", "low"}
PROJECT_EVENTS = {"shortlisted", "used", "validated", "rejected"}
LIBRARY_EVENTS = {"paused", "reopened"}
LIST_FIELDS = {
    "human_truth", "mechanisms", "material_types", "dramatic_functions",
    "memory_carriers", "relationships", "emotions", "themes",
    "visible_evidence", "extensions", "boundaries",
}
SEARCH_WEIGHTS = {
    "mechanisms": 5,
    "dramatic_functions": 4,
    "human_truth": 4,
    "relationships": 3,
    "material_types": 2,
    "emotions": 2,
    "themes": 1,
    "memory_carriers": 1,
}
COMPACT_FIELDS = (
    "id", "source_id", "atom", "human_truth", "mechanisms", "material_types",
    "dramatic_functions", "memory_carriers", "relationships", "emotions", "themes",
    "fact_confidence", "analysis_confidence",
)


class ValidationError(ValueError):
    """Raised when a write would violate the material-store contract."""


def _now() -> datetime:
    return datetime.now().astimezone()


def _require(record: dict, field: str, expected_type: type) -> object:
    value = record.get(field)
    if not isinstance(value, expected_type):
        raise ValidationError(f"{record.get('id', '<new>')} field {field} must be {expected_type.__name__}")
    return value


def _require_enum(record: dict, field: str, allowed: set[str]) -> str:
    value = _require(record, field, str)
    if value not in allowed:
        raise ValidationError(f"{record.get('id', '<new>')} field {field} has invalid value: {value}")
    return value


def load_records(path: Path) -> list[dict]:
    if not path.exists():
        return []
    records: list[dict] = []
    with path.open("r", encoding="utf-8") as handle:
        for line_number, line in enumerate(handle, start=1):
            if not line.strip():
                continue
            try:
                value = json.loads(line)
            except json.JSONDecodeError as exc:
                raise ValidationError(f"invalid JSON at line {line_number}: {exc.msg}") from exc
            if not isinstance(value, dict):
                raise ValidationError(f"line {line_number} must contain a JSON object")
            records.append(value)
    return records


def validate_records(records: list[dict]) -> None:
    seen: set[str] = set()
    source_ids: set[str] = set()
    atom_ids: set[str] = set()
    for record in records:
        record_type = _require(record, "record_type", str)
        if record_type not in {"source", "atom", "usage"}:
            raise ValidationError(f"unsupported record_type: {record_type}")
        record_id = _require(record, "id", str)
        if not ID_PATTERN.fullmatch(record_id):
            raise ValidationError(f"invalid record id: {record_id}")
        if record_id in seen:
            raise ValidationError(f"duplicate record id: {record_id}")
        seen.add(record_id)
        _require(record, "created_at", str)
        if record_type == "source":
            if not _require(record, "original", str).strip():
                raise ValidationError(f"{record_id} original cannot be empty")
            _require_enum(record, "source_type", SOURCE_TYPES)
            _require_enum(record, "fact_status", FACT_STATUS)
            _require_enum(record, "curation_priority", CURATION)
            _require_enum(record, "status", SOURCE_STATUS)
            if _require(record, "privacy", str) != "private":
                raise ValidationError(f"{record_id} privacy must be private")
            for field in ("source_url", "source_title", "rights_note"):
                _require(record, field, str)
            source_ids.add(record_id)
        elif record_type == "atom":
            atom_ids.add(record_id)

    for record in records:
        record_type = record["record_type"]
        record_id = record["id"]
        if record_type == "atom":
            if _require(record, "source_id", str) not in source_ids:
                raise ValidationError(f"{record_id} references missing source")
            if not _require(record, "atom", str).strip():
                raise ValidationError(f"{record_id} atom cannot be empty")
            for field in LIST_FIELDS:
                values = _require(record, field, list)
                if any(not isinstance(value, str) or not value.strip() for value in values):
                    raise ValidationError(f"{record_id} field {field} must contain non-empty strings")
            _require(record, "non_replaceable", str)
            _require(record, "fit", dict)
            _require_enum(record, "fact_confidence", FACT_CONFIDENCE)
            _require_enum(record, "analysis_confidence", ANALYSIS_CONFIDENCE)
            _require_enum(record, "status", ATOM_STATUS)
        elif record_type == "usage":
            ids = _require(record, "atom_ids", list)
            if not ids or any(not isinstance(atom_id, str) or atom_id not in atom_ids for atom_id in ids):
                raise ValidationError(f"{record_id} has missing or invalid atom_ids")
            scope = _require(record, "scope", str)
            event = _require(record, "event_type", str)
            if scope == "project" and event not in PROJECT_EVENTS:
                raise ValidationError(f"{record_id} project scope cannot use event {event}")
            if scope == "library" and event not in LIBRARY_EVENTS:
                raise ValidationError(f"{record_id} library scope cannot use event {event}")
            if scope not in {"project", "library"}:
                raise ValidationError(f"{record_id} has invalid scope: {scope}")
            for field in (
                "project", "script_position", "usage_role", "transformation", "result", "evidence_path",
            ):
                _require(record, field, str)


def _next_id(records: list[dict], prefix: str, date: str) -> str:
    start = f"{prefix}-{date}-"
    numbers = [int(record["id"].rsplit("-", 1)[1]) for record in records if record.get("id", "").startswith(start)]
    return f"{start}{max(numbers, default=0) + 1:03d}"


@contextmanager
def _exclusive_lock(path: Path) -> Iterator[None]:
    digest = hashlib.sha256(str(path.resolve()).encode("utf-8")).hexdigest()[:20]
    lock_path = Path(tempfile.gettempdir()) / f"laohu-story-material-{digest}.lock"
    with lock_path.open("a+", encoding="utf-8") as handle:
        fcntl.flock(handle.fileno(), fcntl.LOCK_EX)
        try:
            yield
        finally:
            fcntl.flock(handle.fileno(), fcntl.LOCK_UN)


def _write_records(path: Path, records: list[dict]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    temporary: Path | None = None
    try:
        with tempfile.NamedTemporaryFile(
            "w", encoding="utf-8", dir=path.parent, prefix=f".{path.name}.", delete=False
        ) as handle:
            temporary = Path(handle.name)
            os.chmod(temporary, 0o600)
            for record in records:
                handle.write(json.dumps(record, ensure_ascii=False, separators=(",", ":")) + "\n")
            handle.flush()
            os.fsync(handle.fileno())
        os.replace(temporary, path)
    finally:
        if temporary is not None and temporary.exists():
            temporary.unlink()


def add_source(path: Path, payload: dict) -> dict:
    if payload.get("record_type") not in {None, "source"}:
        raise ValidationError("add_source cannot ingest non-source records")
    with _exclusive_lock(path):
        records = load_records(path)
        moment = _now()
        record = dict(payload)
        record.update(
            record_type="source",
            id=_next_id(records, "SRC", moment.strftime("%Y%m%d")),
            created_at=moment.isoformat(timespec="seconds"),
        )
        updated = [*records, record]
        validate_records(updated)
        _write_records(path, updated)
        return record


def add_atoms(path: Path, payloads: list[dict]) -> list[dict]:
    if not payloads:
        raise ValidationError("add_atoms requires at least one atom")
    if any(payload.get("record_type") not in {None, "atom"} for payload in payloads):
        raise ValidationError("add_atoms cannot ingest non-atom records")
    with _exclusive_lock(path):
        records = load_records(path)
        moment = _now()
        created: list[dict] = []
        for payload in payloads:
            record = dict(payload)
            record.update(
                record_type="atom",
                id=_next_id([*records, *created], "ATM", moment.strftime("%Y%m%d")),
                created_at=moment.isoformat(timespec="seconds"),
            )
            created.append(record)
        updated = [*records, *created]
        validate_records(updated)
        _write_records(path, updated)
        return created


def log_usage(path: Path, payload: dict) -> dict:
    if payload.get("record_type") not in {None, "usage"}:
        raise ValidationError("log_usage cannot ingest non-usage records")
    with _exclusive_lock(path):
        records = load_records(path)
        moment = _now()
        record = dict(payload)
        record.update(
            record_type="usage",
            id=_next_id(records, "USE", moment.strftime("%Y%m%d")),
            created_at=moment.isoformat(timespec="seconds"),
        )
        updated = [*records, record]
        validate_records(updated)
        _write_records(path, updated)
        return record


def _effective_status(atom: dict, usage: list[dict]) -> str:
    status = atom["status"]
    if status != "callable":
        return status
    for event in usage:
        if event["scope"] != "library" or atom["id"] not in event["atom_ids"]:
            continue
        status = "paused" if event["event_type"] == "paused" else "callable"
    return status


def _values(record: dict, field: str, source: dict, effective_status: str) -> set[str]:
    if field == "curation_priority":
        value = source["curation_priority"]
    elif field == "effective_status":
        value = effective_status
    else:
        value = record.get(field, [])
    if isinstance(value, str):
        return {value}
    if isinstance(value, list):
        return {item for item in value if isinstance(item, str)}
    return set()


def search_records(path: Path, query: dict) -> list[dict]:
    records = load_records(path)
    validate_records(records)
    sources = {record["id"]: record for record in records if record["record_type"] == "source"}
    atoms = [record for record in records if record["record_type"] == "atom"]
    usage = [record for record in records if record["record_type"] == "usage"]
    must = query.get("must", {})
    prefer = query.get("prefer", {})
    if not isinstance(must, dict) or not isinstance(prefer, dict):
        raise ValidationError("search must and prefer must be objects")
    for section_name, section in (("must", must), ("prefer", prefer)):
        for field, wanted in section.items():
            if not isinstance(wanted, list) or any(not isinstance(value, str) for value in wanted):
                raise ValidationError(f"search {section_name}.{field} must be an array of strings")
    exclude_ids = set(query.get("exclude_ids", []))
    limit = max(1, int(query.get("limit", 20)))
    per_source_limit = max(1, int(query.get("per_source_limit", 2)))
    candidates: list[dict] = []
    for atom in atoms:
        source = sources[atom["source_id"]]
        status = _effective_status(atom, usage)
        if source["status"] != "active" or status != "callable" or atom["id"] in exclude_ids:
            continue
        if any(
            not (_values(atom, field, source, status) & set(wanted))
            for field, wanted in must.items()
        ):
            continue
        score = sum(
            SEARCH_WEIGHTS.get(field, 1) * len(_values(atom, field, source, status) & set(wanted))
            for field, wanted in prefer.items()
        )
        project_events = [event for event in usage if event["scope"] == "project" and atom["id"] in event["atom_ids"]]
        item = {field: atom[field] for field in COMPACT_FIELDS}
        item.update(
            curation_priority=source["curation_priority"],
            effective_status=status,
            usage_summary={
                "shortlisted": sum(event["event_type"] == "shortlisted" for event in project_events),
                "used": sum(event["event_type"] == "used" for event in project_events),
                "validated": sum(event["event_type"] == "validated" for event in project_events),
                "rejected": sum(event["event_type"] == "rejected" for event in project_events),
                "latest_result": project_events[-1]["result"] if project_events else "",
            },
            score=score,
        )
        candidates.append(item)
    candidates.sort(
        key=lambda item: (
            -item["score"],
            item["curation_priority"] != "laohu_selected",
            item["id"],
        )
    )
    selected: list[dict] = []
    source_counts: Counter[str] = Counter()
    for item in candidates:
        if source_counts[item["source_id"]] >= per_source_limit:
            continue
        selected.append(item)
        source_counts[item["source_id"]] += 1
        if len(selected) >= limit:
            break
    return selected


def get_records(path: Path, atom_ids: list[str]) -> dict:
    records = load_records(path)
    validate_records(records)
    wanted = set(atom_ids)
    atoms = [record for record in records if record["record_type"] == "atom" and record["id"] in wanted]
    if {atom["id"] for atom in atoms} != wanted:
        missing = sorted(wanted - {atom["id"] for atom in atoms})
        raise ValidationError("unknown atom ids: " + ", ".join(missing))
    source_ids = {atom["source_id"] for atom in atoms}
    return {
        "atoms": atoms,
        "sources": [record for record in records if record["record_type"] == "source" and record["id"] in source_ids],
        "usage": [record for record in records if record["record_type"] == "usage" and wanted.intersection(record["atom_ids"])],
    }


def stats(path: Path) -> dict:
    records = load_records(path)
    validate_records(records)
    sources = [record for record in records if record["record_type"] == "source"]
    source_by_id = {source["id"]: source for source in sources}
    atoms = [record for record in records if record["record_type"] == "atom"]
    usage = [record for record in records if record["record_type"] == "usage"]
    project_used = {
        atom_id
        for event in usage
        if event["scope"] == "project"
        for atom_id in event["atom_ids"]
    }
    callable_atoms = [
        atom
        for atom in atoms
        if source_by_id[atom["source_id"]]["status"] == "active"
        and _effective_status(atom, usage) == "callable"
    ]
    top: dict[str, list[list[object]]] = {}
    for field in ("relationships", "mechanisms", "dramatic_functions", "themes", "emotions"):
        counts = Counter(value for atom in atoms for value in atom[field])
        top[field] = [[value, count] for value, count in counts.most_common(10)]
    return {
        "source_count": len(sources),
        "atom_count": len(atoms),
        "callable_count": len(callable_atoms),
        "usage_count": len(usage),
        "recent_atom_ids": [
            atom["id"] for atom in sorted(atoms, key=lambda item: item["created_at"], reverse=True)[:10]
        ],
        "long_unused_atom_ids": [atom["id"] for atom in callable_atoms if atom["id"] not in project_used][:10],
        "top": top,
    }


def _read_payload(location: str) -> object:
    text = sys.stdin.read() if location == "-" else Path(location).read_text(encoding="utf-8")
    return json.loads(text)


def _parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--db", type=Path, default=DEFAULT_DB)
    subparsers = parser.add_subparsers(dest="command", required=True)
    for name in ("add-source", "add-atoms", "search", "log-usage"):
        command = subparsers.add_parser(name)
        command.add_argument("--input", required=True)
    get = subparsers.add_parser("get")
    get.add_argument("--id", action="append", required=True)
    subparsers.add_parser("stats")
    subparsers.add_parser("validate")
    return parser


def main(argv: list[str] | None = None) -> int:
    args = _parser().parse_args(argv)
    try:
        if args.command == "add-source":
            payload = _read_payload(args.input)
            if not isinstance(payload, dict):
                raise ValidationError("add-source input must be an object")
            result = add_source(args.db, payload)
        elif args.command == "add-atoms":
            payload = _read_payload(args.input)
            if not isinstance(payload, list) or any(not isinstance(item, dict) for item in payload):
                raise ValidationError("add-atoms input must be an array of objects")
            result = add_atoms(args.db, payload)
        elif args.command == "search":
            payload = _read_payload(args.input)
            if not isinstance(payload, dict):
                raise ValidationError("search input must be an object")
            result = search_records(args.db, payload)
        elif args.command == "get":
            result = get_records(args.db, args.id)
        elif args.command == "log-usage":
            payload = _read_payload(args.input)
            if not isinstance(payload, dict):
                raise ValidationError("log-usage input must be an object")
            result = log_usage(args.db, payload)
        elif args.command == "stats":
            result = stats(args.db)
        else:
            records = load_records(args.db)
            validate_records(records)
            result = {"ok": True, "record_count": len(records)}
        print(json.dumps(result, ensure_ascii=False, indent=2))
        return 0
    except (ValidationError, json.JSONDecodeError) as exc:
        print(json.dumps({"ok": False, "error": str(exc)}, ensure_ascii=False), file=sys.stderr)
        return 2
    except OSError as exc:
        print(json.dumps({"ok": False, "error": str(exc)}, ensure_ascii=False), file=sys.stderr)
        return 1


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2：核对压缩搜索、详情读取和统计接口**

`search_records` 接受：

```json
{
  "must": {"relationships": ["父子"]},
  "prefer": {
    "mechanisms": ["用解决实际问题代替直接表达关心"],
    "dramatic_functions": ["character_setup"]
  },
  "exclude_ids": [],
  "limit": 20,
  "per_source_limit": 2
}
```

固定权重：`mechanisms=5`、`dramatic_functions=4`、`human_truth=4`、`relationships=3`、`material_types=2`、`emotions=2`、`themes=1`、`memory_carriers=1`。严格、放宽和类比不是脚本内的隐藏模式，而是素材 Skill 每轮改变 `must/prefer` 并记录放宽字段；脚本只做确定性过滤、评分和投影。

搜索输出只允许：

```text
id / source_id / atom / human_truth / mechanisms / material_types /
dramatic_functions / memory_carriers / relationships / emotions / themes /
curation_priority / fact_confidence / analysis_confidence /
effective_status / usage_summary / score
```

不得出现 `original / visible_evidence / extensions / boundaries / transformation`。`get_records` 只返回指定 atom、关联 source 和相关 usage；`stats` 只返回计数与高频标签。

- [ ] **Step 3：增加 CLI 子命令**

CLI 固定为：

```text
add-source --input <json-file|->
add-atoms --input <json-file|->
search --input <query-json-file|->
get --id ATM-20260829-001 [--id ATM-20260829-002]
log-usage --input <json-file|->
stats
validate
```

所有子命令支持 `--db`，默认路径从脚本的 `parents[3]` 解析为 `00_输入原料/故事素材原子库.jsonl`。成功输出 UTF-8 JSON；校验失败输出 `{"ok": false, "error": "validation message"}` 到 stderr 并返回 2；其他运行错误返回 1。

- [ ] **Step 4：运行数据库测试直到 GREEN**

Run: `python3 -m unittest tests.test_story_material_db -v`

Expected: 6 tests PASS。

- [ ] **Step 5：验证 CLI 和语法**

Run: `python3 -m py_compile skills/laohu-story-material/scripts/story_material_db.py`

Expected: exit 0，无输出。

Run: `python3 skills/laohu-story-material/scripts/story_material_db.py --db /tmp/laohu-story-material-plan-test.jsonl stats`

Expected: JSON 中 `source_count=0`、`atom_count=0`、`callable_count=0`，且不创建项目正式素材库。

### Task 4：创建 `laohu-story-material` Skill

**Files:**
- Create: `skills/laohu-story-material/SKILL.md`
- Create: `skills/laohu-story-material/agents/openai.yaml`
- Create: `skills/laohu-story-material/references/01_原子记录与渐进检索合同.md`
- Test: `tests/test_story_material_architecture.py`

- [ ] **Step 1：写入 Skill 入口**

`SKILL.md` 必须包含以下 frontmatter：

```yaml
---
name: laohu-story-material
description: Use when 老胡明确说“记录灵感、保存素材、记一下这个案例、放进素材库”等，需要把生活观察、短视频案例、对话或突发想法长期保存并拆成可检索故事原子；也用于正式编剧任务调用素材库、逐级放宽检索或形成待确认的外部候选报告。用户只是在分享、讨论或要求当前创作而没有保存意图时，不自动长期收录。
---
```

主体使用领域标题，不出现机械四层栏目，并完整写入以下执行合同：

```text
# 老胡故事素材库
## 素材箴言
原话留住人，原子帮助故事重新长出来。

## 职责与触发边界
- 明确保存意图才收录；分享、讨论和当前创作不擅自长期保存。
- 独占 source / atom / usage 拆解、检索、充分度、外部候选报告和 usage 回流。
- 不写正式剧本，不替编剧决定高潮，不调用媒体生成。

## 收录工作流
逐字保存→事实/推论/感受/未知→独立价值核心→人性机制与可见证据→延展与边界→脚本写入→简短回执。
原话只保存一次；多个 atom 只引用 source_id。
只有抽象结论没有证据时使用 pending_evidence。

## 渐进检索
第一层 stats；第二层 search 压缩索引；第三层通常读取3—7个候选详情但不设最低数量；第四层形成创作交接。
严格匹配→每轮只放宽一个表面条件→跨领域类比→内部缺口报告。
充分度看剧本缺口是否解决，不看固定命中数；允许零采用。

## 外部候选确认门
前三层不足才执行网络搜索；结果保持 external_candidate / not_approved。
报告原因、来源、机制、证据、艺术化改造、不可复制部分、事实/版权/隐私风险和建议。
未经老胡确认不得进入剧本或长期素材库。

## 编剧交接与使用回流
素材交接包写保留机制、允许改变、不可复制、适合位置、预期改变和不用它会损失什么。
素材调用回执写查询、候选、采用/不采用、角色、改造和可观察变化。
项目 rejected 不停用全库；只有 library paused/reopened 改变调用状态。

## 工具与验收
真实写入、搜索、详情和 usage 必须调用 scripts/story_material_db.py。
读取 01_原子记录与渐进检索合同.md 的触发条件和停止条件。
验收原话完整、单真源、索引不泄露全文、外部不越权、零采用不造假。
```

- [ ] **Step 2：写入唯一条件性 Reference**

`references/01_原子记录与渐进检索合同.md` 按设计文档第六、九、十一、十二、十三、十七节写成运行时规范，必须包含：

```text
source / atom / usage 三种完整字段表与枚举；
每个字段何时必填、何时 unknown、何时不得猜测；
策展优先级、事实置信度和分析置信度分别回答审美接近度、现实可信度和机制解释可靠度，不能合并成一个总分；
四层加载各自允许输出和禁止输出；
查询合同、逐轮放宽记录、充分度判断；
外部候选报告模板，以及五种明确决策：批准当前使用并入长期库 / 只允许当前参考 / 只保留为对照 / 拒绝 / 继续搜索其他方向；
项目级与素材库级 usage 的分权；
临时 JSON 输入文件放在 tmp/，写入后删除，不把原话写入命令行参数；
数据库不存在时：采集创建，检索报告为空但允许编剧继续原创。
```

Skill 路由表只在“首次写入、字段不清、放宽检索、外部候选、状态回流或工具报错”时读取该 Reference；普通 stats 不默认读取全文。

- [ ] **Step 3：写入 UI 元数据**

```yaml
interface:
  display_name: "老胡故事素材库"
  short_description: "保存生活灵感，拆成故事原子，并为正式剧本渐进检索"
  default_prompt: "Use $laohu-story-material to preserve this material exactly, extract reusable story atoms, or retrieve fitting atoms for a screenplay without forcing adoption."
```

- [ ] **Step 4：验证 Skill 结构**

Run: `python3 /Users/a1/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/laohu-story-material`

Expected: `Skill is valid!`

- [ ] **Step 5：运行架构测试，确认只剩跨 Skill 和文档失败**

Run: `python3 -m unittest tests.test_story_material_architecture.StoryMaterialArchitectureTests.test_story_material_is_an_independent_owner -v`

Expected: PASS。

### Task 5：接通总控、编剧和复盘

**Files:**
- Modify: `skills/laohu-ai-visual/SKILL.md`
- Modify: `skills/laohu-script-writer/SKILL.md`
- Modify: `skills/laohu-generation-review/SKILL.md`
- Test: `tests/test_story_material_architecture.py`

- [ ] **Step 1：在总控增加明确保存路由**

在“按最上游缺口路由”增加：

```markdown
| 明确说“记录灵感、保存素材、记一下这个案例、放进素材库”，或正式编剧需要检索长期素材 | `laohu-story-material` | 原话完整保存并拆成可检索原子，或形成经过充分度判断的素材交接包；外部候选仍停在确认门 |
```

在混合输入说明：一次输入同时要求保存与写剧本时，先由素材 Skill 写入，再把本次新原子连同检索结果交给编剧；没有明确保存意图时不得因为内容“看起来有价值”而自动入库。

- [ ] **Step 2：在编剧增加素材查询合同**

在“剧本开发”正式开发顺序之前加入：

```text
【素材查询合同】
当前剧本缺口：
素材职责：主故事 / 冲突 / 人物行为 / 台词 / 场景质感 / 物件 / 记忆点 / 价值信息。
当前人物关系与选择压力：
希望加强的人性机制或情绪：
必须保护的故事事实：
候选需要在哪些因果方向上不同：
怎样算已经充分：
禁止套路与不适配：
```

执行规则必须写清：

```text
正式故事设计、分场、单场次或重写开始前调用 laohu-story-material。
先严格匹配；不足时每轮只放宽一个表面条件；再做跨领域类比；仍不足才形成外部缺口报告。
同一任务且人物、事实、职责和缺口未变时复用素材交接包；条件改变或新任务重新查询。
候选足够时停止；只采用一条不是不足；允许零采用。
未经确认的 external_candidate / not_approved 不得进入剧本。
```

在输出合同增加：

```text
【素材调用回执】
检索条件与放宽记录：
候选 atom IDs：
采用 / 不采用理由：
保留机制 / 改变人物处境 / 不可复制：
进入剧本的位置和职责：
对观众理解、期待、关系感受或情绪的可观察变化：

【记忆点账本】
位置 / atom ID / 主职责 / 观众状态变化 / 与主线因果 / 相邻竞争 / 是否需要呼吸。
```

同时写明：不设置“每几秒一个记忆点”的固定规则。短内容可以提高变化密度，长内容可以延长铺垫；验收看它是否改变观众理解、期待、关系感受或情绪，主记忆点是否高于支撑细节，以及留白是否服务后续兑现。

- [ ] **Step 3：在复盘增加素材首断点回流**

在首断点表加入：

```markdown
| 剧本加入了真实细节却不适配人物、改造后丢失原机制、多个记忆点互相竞争或素材误导事实 | 先比较素材交接包、正式剧本位置与真实结果，区分素材本身、检索选择、编剧改造和下游执行 | 当前作品返编剧；只有素材选择或适用边界确为首断点时交 `laohu-story-material` 追加项目级 usage，不能直接暂停全库原子 |
```

长期回流写明：单次 rejected 只记录当前作品；重复、证据充分且影响库级可调用性时，才建议 library `paused`，由素材负责人执行。

- [ ] **Step 4：运行三项跨 Skill 测试**

Run: `python3 -m unittest tests.test_story_material_architecture.StoryMaterialArchitectureTests.test_router_saves_only_when_the_user_expresses_save_intent tests.test_story_material_architecture.StoryMaterialArchitectureTests.test_script_writer_must_query_but_may_adopt_nothing tests.test_story_material_architecture.StoryMaterialArchitectureTests.test_review_returns_material_evidence_to_the_material_owner -v`

Expected: 3 tests PASS。

### Task 6：更新十四项能力合约、地图、公开入口和隐私边界

**Files:**
- Modify: `.gitignore`
- Modify: `AGENTS.md`
- Modify: `02_共享资产库/00_核心规则手册.md`
- Modify: `02_共享资产库/05_工具流程/laohu_skills核心合约.md`
- Modify: `README.md`
- Modify: `输入输出索引.md`
- Modify: `skills/laohu-ai-visual/scripts/check_laohu_skills.sh`
- Modify: `scripts/validate_capability_architecture.py`

- [ ] **Step 1：保护私有 JSONL**

在 `.gitignore` 的本地状态段增加：

```gitignore
# Private story observations and source transcripts stay local.
/00_输入原料/故事素材原子库.jsonl
```

不要忽略整个 `00_输入原料/`，以免公开可复用输入说明失去追踪。

- [ ] **Step 2：更新顶层十四项能力**

把所有“十三个 Skill / 十三项能力”更新为十四；“十二个生产、专业设计与复盘能力，加一个长期能力进化入口”改为“十三个生产、素材、专业设计与复盘能力，加一个长期能力进化入口”。在负责人表中把新 Skill 放在总控与编剧之间：

```markdown
| `laohu-story-material` | 在明确保存意图下完整保存原话、拆成故事原子；为正式编剧执行渐进检索、充分度判断、外部候选隔离和使用回流 | source / atom / usage 可验证，素材交接包成立，未经确认外部候选未越权 |
```

路由表增加“长期保存、素材检索或内部素材缺口”。顶层只写职责、边界和完成标志，不复制 JSON 字段和评分权重。

- [ ] **Step 3：更新能力地图和核心合约**

能力地图增加：

```markdown
| 日常灵感没有长期保存，或正式剧本缺少可调用的真实生活材料 | laohu-story-material | 原话完整、原子可检索、候选经过充分度与边界判断并能交给编剧 | 不写正式剧本，不把未批准网络候选写入作品或正式库 |
```

核心合约增加负责人行、`素材 → 编剧` 交接段和失败返回：

```text
素材来源和 source ID：
候选 atom IDs：
当前剧本缺口与检索层级：
保留机制和可见证据：
允许改变 / 不可复制：
适合位置和主职责：
充分度结论：已解决 / 仍缺口。
外部候选状态：无 / external_candidate / not_approved / 已批准范围。
```

作品 HTML 合同中的“十四个 Skill”只指会写作品阶段 Markdown 的业务 Skill；素材数据库本身不渲染 HTML，也不在交付中心暴露原话。

- [ ] **Step 4：更新 README 和输入输出索引**

README 在“从灵感找到故事”后增加“记录生活素材并在写剧本时调用”，说明明确触发、完整原话、渐进检索、零采用和外部确认。核心 Skill 表加入链接。

输入输出索引把“十三个核心 Skill”改为“十四个核心 Skill”，加入 `laohu-story-material`、其 Reference 和数据库脚本；明确私有 JSONL 不是公开共享资产。

- [ ] **Step 5：更新总检查的数量和隐私断言**

`core_skills` 在总控后加入 `laohu-story-material`；数量断言由 13 改为 14，提示文本同步改为 fourteen。

在 `validate_capability_architecture.py` 的索引检查后增加：

```python
    private_material = "00_输入原料/故事素材原子库.jsonl"
    gitignore = (ROOT / ".gitignore").read_text(encoding="utf-8")
    if f"/{private_material}" in gitignore:
        passed("private story material database is ignored")
    else:
        fail("private story material database is not ignored", failures)

    tracked = subprocess.run(
        ["git", "ls-files", "--error-unmatch", private_material],
        cwd=ROOT,
        text=True,
        capture_output=True,
        check=False,
    )
    if tracked.returncode != 0:
        passed("private story material database is not tracked")
    else:
        fail("private story material database is tracked", failures)
```

同时在 imports 加入 `subprocess`。

- [ ] **Step 6：运行文档与隐私架构测试**

Run: `python3 -m unittest tests.test_story_material_architecture.StoryMaterialArchitectureTests.test_private_database_is_ignored_and_untracked tests.test_story_material_architecture.StoryMaterialArchitectureTests.test_project_documents_register_fourteen_owners -v`

Expected: 2 tests PASS。

### Task 7：把行为场景接入现有能力验证

**Files:**
- Modify: `tests/capability_scenarios.json`
- Test: `scripts/validate_capability_architecture.py`

- [ ] **Step 1：登记新 Skill 合同**

在 `skill_contracts` 增加：

```json
"laohu-story-material": {
  "headings": ["素材箴言", "职责与触发边界", "收录工作流", "渐进检索", "外部候选确认门", "编剧交接与使用回流", "工具与验收"],
  "anchors": ["明确保存意图", "原话只保存一次", "source / atom / usage", "严格匹配", "放宽表面条件", "跨领域类比", "允许零采用", "external_candidate / not_approved", "未经老胡确认不得进入剧本或长期素材库", "素材调用回执", "项目级", "素材库级", "story_material_db.py"]
}
```

- [ ] **Step 2：增加四条端到端场景**

```json
{
  "name": "明确记录灵感先保存原话再拆成多个故事原子",
  "chain": [
    {"owner": "laohu-ai-visual", "anchors": ["记录灵感", "laohu-story-material", "不擅自长期保存"]},
    {"owner": "laohu-story-material", "anchors": ["原话只保存一次", "source / atom / usage", "简短回执"]}
  ]
},
{
  "name": "正式编剧先渐进检索素材但允许零采用",
  "chain": [
    {"owner": "laohu-story-material", "anchors": ["严格匹配", "放宽表面条件", "跨领域类比", "允许零采用"]},
    {"owner": "laohu-script-writer", "anchors": ["素材查询合同", "素材调用回执", "记忆点账本"]}
  ]
},
{
  "name": "内部素材不足后网络候选必须等待老胡确认",
  "chain": [
    {"owner": "laohu-story-material", "anchors": ["内部缺口报告", "external_candidate / not_approved", "未经老胡确认不得进入剧本或长期素材库"]},
    {"owner": "laohu-script-writer", "anchors": ["未经确认", "不得进入剧本"]}
  ]
},
{
  "name": "素材失败证据按项目和素材库两个范围回流",
  "chain": [
    {"owner": "laohu-generation-review", "anchors": ["素材选择", "项目级 usage", "不能直接暂停全库原子"]},
    {"owner": "laohu-story-material", "anchors": ["项目 rejected 不停用全库", "library paused/reopened"]}
  ]
}
```

- [ ] **Step 3：增加一条运行时 Reference 可达场景**

```json
{
  "name": "长口述素材只保存一次并按需读取候选原话",
  "owner": "laohu-story-material",
  "request": "这段是我生活里观察到的长口述，记录灵感；以后写父子关系时可以用，但不要把真实人物和原话照搬。",
  "required_references": ["skills/laohu-story-material/references/01_原子记录与渐进检索合同.md"],
  "required_intermediate": ["逐字保存", "原话只保存一次", "多个 atom", "按 ID", "不可复制"],
  "observable_change": "建立一条 source 和多个职责不同的 atom；普通搜索只看压缩索引，入围后才读取完整原话和边界。",
  "forbidden_shortcut": "把长原话复制到每个原子；每次编剧全库加载；或因为案例有价值就自动公开和长期保存。"
}
```

- [ ] **Step 4：验证 JSON 与能力场景**

Run: `python3 -m json.tool tests/capability_scenarios.json >/dev/null`

Expected: exit 0。

Run: `python3 scripts/validate_capability_architecture.py`

Expected: `Capability architecture validation passed.`

### Task 8：记录能力进化、完成全量回归并建立安全检查点

**Files:**
- Modify: `04_诊断与系统日志/能力进化台账.md`
- Verify: all files above

- [ ] **Step 1：追加 `EVO-20260829-003`**

记录必须包含现有验证器要求的十二个字段，并使用以下事实：

```text
状态：KEEP（只证明文本架构、数据库不变量和行为场景成立，不声称真实素材已提高成片传播）。
进入方式与来源：FEEDBACK + CAPABILITY_GAP；老胡明确要求长期积累、渐进加载、编剧必查、逐级放宽和外部候选人工确认。
证据指纹：长素材卡不可扩展 + DB Skills原子JSONL启发 + 原话/原子/使用历史混写 + 编剧无长期检索入口 + 素材不足时外部搜索越权风险。
第一个能力断点：现有编剧只处理当前灵感，没有跨作品素材的独立保存、检索、充分度与回流负责人。
唯一规则负责人：laohu-story-material；总控只路由，编剧只消费和改造，复盘只交证据。
必须保护：明确触发才保存、私有作品不进Git、编剧人物选择与因果权不被素材库接管、外部案例只提炼机制。
旧行为：长 Markdown 卡片、每次全读、无单真源、无外部确认门。
新行为：单 JSONL、source/atom/usage、四层加载、四级检索、零采用、外部隔离、追加事件。
写回位置：列出本计划全部新建与修改文件。
行为验证：数据库6项、架构7项、能力场景、Skill validator、全量 unittest 和总检查结果；若总检查只剩执行前已有 `.DS_Store`，必须如实记录为既有卫生问题而非本功能通过。
重新唤醒条件：素材规模导致查询真实变慢；索引泄露原话；AI把未确认网络素材写入剧本；项目 rejected 错误停用全库；剧本检索退化为机械塞素材；真实成片证明记忆点互相竞争。
版本动作：只允许本地检查点，不 push/PR/Release；重叠脏文件不混合提交。
```

- [ ] **Step 2：运行完整单元测试**

Run: `python3 -m unittest discover -s tests -p 'test_*.py'`

Expected: 原有 21 项加新架构 7 项和数据库 6 项全部通过，共 34 项；若实际测试数因执行时现有分支新增测试而变化，以“0 failures / 0 errors”为准并记录总数。

- [ ] **Step 3：运行结构、Skill 和语法验证**

Run: `python3 /Users/a1/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/laohu-story-material`

Expected: `Skill is valid!`

Run: `python3 -m py_compile skills/laohu-story-material/scripts/story_material_db.py scripts/validate_capability_architecture.py`

Expected: exit 0。

Run: `python3 scripts/validate_capability_architecture.py`

Expected: `Capability architecture validation passed.`

Run: `bash skills/laohu-ai-visual/scripts/check_laohu_skills.sh`

Expected: 新增 Skill、数量、能力场景和数据库隐私断言全部 PASS。若命令只因执行前已经存在的公共 `.DS_Store` 失败，保留失败并在交付中单列，不擅自删除用户文件；任何其他失败必须修复后重跑。

- [ ] **Step 4：做隐私和范围机械检查**

Run: `git check-ignore -v '00_输入原料/故事素材原子库.jsonl'`

Expected: 命中 `.gitignore` 中的精确路径。

Run: `git ls-files '00_输入原料/故事素材原子库.jsonl'`

Expected: 无输出。

Run: `git diff --check`

Expected: 本轮涉及文件无空白错误；若其他旧改动有错误，改用逐文件 `git diff --check -- <本轮文件>` 定位，不修改无关文件。

- [ ] **Step 5：审查是否出现第二数据真源或未批准候选**

Run: `rg -n '故事素材.*(INDEX|季度|副本)|ai_external_candidate' skills/laohu-story-material AGENTS.md README.md 输入输出索引.md '02_共享资产库/00_核心规则手册.md' '02_共享资产库/05_工具流程/laohu_skills核心合约.md'`

Expected: 不出现手工索引、季度副本或正式库内 `ai_external_candidate`；Reference 可以说明该值必须被拒绝。

- [ ] **Step 6：建立不混入既有脏改动的本地检查点**

先运行：

Run: `git diff --cached --name-only`

Expected: 无预先暂存文件。若存在，停止提交并报告，不能替用户重置暂存区。

仅新建文件可以单独暂存：

```text
skills/laohu-story-material/SKILL.md
skills/laohu-story-material/agents/openai.yaml
skills/laohu-story-material/references/01_原子记录与渐进检索合同.md
skills/laohu-story-material/scripts/story_material_db.py
tests/test_story_material_architecture.py
tests/test_story_material_db.py
```

Run: `git add -- <上述六个新文件>`

Run: `git diff --cached --name-only`

Expected: 只出现上述六个文件。

Run: `git commit -m 'feat: add story material atom library core' -- <上述六个新文件>`

Expected: 本地提交成功。`.gitignore`、现有 Skill、合约、地图、场景、验证器、README、索引和台账因为执行前已有未提交修改，不纳入这个提交；在最终交付中明确它们仍是工作树增量。

## 完成后的人工验收

1. 发一段不少于 1,000 字的虚构长口述并说“记录灵感”，确认一条 source、多条 atom、原话不截断、聊天只给简短回执。
2. 不说保存，只讨论同一段口述，确认不创建数据库记录。
3. 发一个正式剧本任务，确认先有素材查询合同，再看压缩候选，只有入围 ID 才加载原话；可以零采用。
4. 让严格匹配不足，确认系统逐轮说明放宽了什么；最终只采用一条强素材时不触发网络搜索。
5. 让内部确实无素材，确认网络结果先形成 `external_candidate / not_approved` 报告，停在老胡确认门。
6. 把某原子在一部作品中标记 rejected，确认它仍可被其他项目检索；只有 library paused 才隐藏，reopened 后恢复。

真实生活素材的首次正式录入不属于架构实施测试。只有老胡明确给出“记录灵感”等保存意图时，才写入正式 `00_输入原料/故事素材原子库.jsonl`。
