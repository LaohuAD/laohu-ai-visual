# 老胡 AI 视觉：目录与内容整合需求说明书

> **这份文件是什么**：一份自包含的任务说明书，交给另一个 AI 执行。它不依赖任何前置对话，读完即可开工。
> **这份文件不是什么**：不是技术方案，不是执行记录，不是目录规范。它只说"老胡要什么、判断标准是什么、怎么验收"。
> **项目根目录**：`/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/`

---

## 一、最终目的

**把 `/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/` 整理成"目录层级 = 调用层级"的结构，让 AI 拿到用户一句模糊指令后，能一次路由到正确的方法文件。**

展开说，要同时满足四件事：

```text
1. 目录能一眼看出哪个文件干什么       → 层级清楚，不东一块西一块
2. AI 不用加载很多文件就能找到方法     → 不拆太细
3. 同一件事只在一个文件里写一份        → 不重复
4. 每个文件的信息量和信息密度不下降    → 整理不等于删减
```

**这不是一次内容创作任务，是一次文件整合任务。** 现有内容只搬运、只合并、只删除多余，**不重写**。

---

## 二、必读文件（开工前按顺序读）

```text
项目顶层规则
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/AGENTS.md

项目的原始整理指令（重要，含验收矩阵与操作规范）
/Volumes/Laohu_Work/下载/老胡AI视觉_现有架构逐层细化与保真整理_执行交接说明_修订版.md

五包职责与公共字段（权威）
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-ai-visual/references/laohu_skills核心合约.md

跨能力协作关系（HANDOFF / CONSULT / REVIEW / RETURN）
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-ai-visual/references/能力协作图谱.md

四层能力的权威定义（本需求的核心判据来源）
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-ai-visual/skills/laohu-capability-evolution/references/02_四层能力重编译.md

外部能力登记（哪些是外来的、哪些不能改）
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-ai-visual/references/外部能力依赖清单.md

全库目录现状（无省略，1549 行）
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/04_诊断与系统日志/完整目录树.md

前几轮已生成的方案与记录（含未完成事项）
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/04_诊断与系统日志/全库整理方案.md
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/04_诊断与系统日志/改造方案.md
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/04_诊断与系统日志/现有架构细化_实施记录.md
```

---

## 三、项目现状

```text
五个板块（第 1 层）
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-ai-visual/          总控：意图、路由、导演、复盘、进化
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/      故事：灵感→剧本→分段
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-image-creation/     图片：静态画面的一切
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-video-prompt/       视频：时间里的一切（含音频）
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-language-mode/      语言：表达与必要信息

规模：119 个 SKILL.md，700+ 个 md 文件
```

**已知问题（全部已核实，不是推测）：**

| # | 问题 | 位置 | 规模 |
|---|---|---|---|
| 1 | `references/` 里出现 `skills/`（受控副本把别的包整棵复制进来） | 各板块 `<板块>/内置方法/` | ai-visual 218 个副本 / 自有 40 个（85%） |
| 2 | 前几轮多建的同级模块文件 | 图片包 15 个、视频包 12 个、摄影 9 个 | 共 36 个 |
| 3 | 剧本特征资产未分解 | `/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/references/高质量AI剧本开发计划.md` | 854 行 |
| 4 | 同一件事在两个文件各写一份（A1 重叠） | 全库，尚未扫描 | 未知 |
| 5 | 101/119 个 SKILL.md 套了机械的「灵魂/筋骨/血肉/表皮」骨架 | 全库 SKILL.md | 101 个 |

---

## 四、必须理解的四个概念（不理解就会做错）

### 4.1 目录层级 = 调用层级，只有三层

```text
第 1 层  板块    <板块>/SKILL.md
                作用：路由。识别任务属于哪个板块，分派下去。

第 2 层  专业    <板块>/skills/<专业>/SKILL.md
                作用：一个能被独立调用、独立验收的能力。
                判据：会不会被本板块以外的请求调用？
                      会 → 值得做第 2 层；不会 → 并进调用它的那个专业。

第 3 层  方法    <专业>/references/<方法>.md
                作用：该专业的方法正文。
                判据：会不会被两个以上专业读？
                      会 → 单独成文件；不会 → 并进读它的那个专业。
```

**只有一种情况再加一层 `skills/`**：该专业下确实有**两个以上各自可独立调用、互不依赖**的子能力（例：音频设计下的配音／音效／环境／配乐／混音）。

**用户的原话表述**：「Skill.md 和 Reference 和 skills 这几个东西是并行的；Reference 文件下面只有一个对应的指导文件；skills 下面才会有 SKILL.md 和 Reference 文件，甚至再有 skills 目录。」

### 4.2 四层能力是**功能归属**，不是四个固定标题

权威定义在 `/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-ai-visual/skills/laohu-capability-evolution/references/02_四层能力重编译.md`：

> **四层是能力完整性的判断方式，不是四个固定标题，也不是每次更新都要填满的表格。**

| 层 | 承载 | 判断问题 |
|---|---|---|
| **灵魂 SOUL** | 价值、方向、取舍 | 什么值得追求、舍弃或保护发生变化了吗 |
| **筋骨 STRUCTURE** | 负责人、关系、顺序、状态、调用链 | 章法不是步骤表，而是整个能力怎样接力 |
| **血肉 CRAFT** | 材料选择、专业方法、条件、效果因果 | 完整技法六步（见下） |
| **表皮 FORM** | 格式、字段、路径、参数、工具、硬边界 | 不把审美偏好写成机械强制 |

**血肉层的完整技法六步（逐项都要有）**：

```text
触发条件
→ 材料承担的关系、情绪、信息或功能
→ 候选材料或方法的比较
→ 为什么选择这一种
→ 它如何改变可见、可听或可执行结果
→ 什么情况下保留原稿或换用其他方法
```

**材料选择四判据**：因果承重、典型性、媒介可见性、实现与维护代价。

**关键推论——判断一句话留不留，标准不是"是不是总结"，而是"它服务哪一层"：**

```text
方向句  「临界点越具体，张力越大」       → 灵魂层（择优梯度）→ 必须保留
表现句  「临界时刻＝话快骂出口、门快关上」 → 血肉层（可见证据）→ 必须保留
复述句  「信息差就是各方知道的不一样」     → 四层都不服务     → 删除
```

前几轮犯过的错：拿"复述句"的判据去砍了"方向句"。**方向句是灵魂层，删掉 AI 就失去了往哪个方向推的判断依据。**

**伪箴言信号（灵魂层反向检查）**：换到任何项目仍然成立；删掉后不影响选择；需要长篇说明才知道它在说什么；只是禁令换行；只有气势，没有因果。

### 4.3 识别**意图**，不是识别**用户的话**

**错误做法**：把"触发"写成用户原话清单（「这人形不好看」「比例怪」「衣服不贴身」）。

**为什么错**：每个人表达方式不同，同一个意图会有无数种说法，写死在原话上，换个人就接不上。

**正确做法**：触发写**意图类别**。

```text
✗ 用户说「这人形不好看」「比例怪」「衣服不贴身」
✓ 意图＝人物成图的身形／比例／贴合度不成立
```

### 4.4 `SKILL.md` 与 `references/` 的分工

权威依据同上文件：

> **每次必做的专业判断进入 Skill；特定题材、模型、输入状态或失败分支进入 Reference。**

```text
SKILL.md
  ├── 路由：什么意图进来、分派给谁
  └── 本身的功能：本专业每次都必做的那几个专业判断（不是只有路由）

<专业>/references/*.md
  └── 特定题材、特定模型、输入状态、失败分支的方法正文
      写法是【正面描述】：这个东西正确应该怎么做
      负面清单（「不要怎么怎么样」）不归 Reference
```

---

## 五、整合操作定义

设：`A.md` 文件里包含 A1、A2、A3 三个部分。范围包括 **SKILL.md 本体**和 **Reference 文件**。

```text
情况一：A1 与 B.md 的内容重叠，或 A1 本来就该归 B
  → 把 A1 的内容【融合】进 B
  → B 的信息密度、信息量因此变大
  → A.md 里减少 A1 这部分

情况二：A 里某部分（如 A2）没有任何文件承接，
        但它被多处调用、值得独立
  → 拆成一个独立文件

情况三：A1 哪都不归，只有 A 会用
  → 留在 A，不动
```

### 5.1「融合」的准确含义

**不是原文倾倒。** 搬过去时必须融合进目标文件原有的结构：

- **编号冲突要重排**（原文件里的"8."搬进新文件后不能还叫 8）
- 语序、衔接、层级可以调整
- **不变的是：信息量、信息密度、原本的意图和内容**

**这不是"文字一字不改"，是"信息一字不减"。** 这两件事必须分清。

### 5.2 判断口诀

```text
先看谁会读 → 只有一个专业读就并进去
并进去会串味 → 在该专业 references/ 下新建一个主题文件
多处在读、无归属 → 拆成独立文件
```

---

## 六、判断标准

### 6.1 每个文件怎么判

```text
被两个以上专业读   → 独立成文件
只被一个专业读     → 并进那个专业已有的 reference
并进去后主题变混   → 在该专业 references/ 下新建
现有目录能承接     → 不新建
一次性产物         → 不留
```

### 6.2 每句话怎么判

```text
服务灵魂（方向、取舍）        → 留
服务筋骨（负责人、顺序、调用链）→ 留
服务血肉（触发、材料、表现、反例）→ 留
服务表皮（字段、路径、参数）   → 留
四层都不服务（同义复述）      → 删
```

### 6.3 新建文件的准入条件（必须满足其一）

```text
1. 会被两个以上专业调用
2. 有独立的判断、输入输出和验收
3. 换掉它，其他部分仍然成立
```

---

## 七、必须处理的已知问题（含绝对路径）

### 7.1 受控副本错位（最高优先）

**现象**：`/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-ai-visual/内置方法/` 等目录里，整棵复制了别的包（含对方的 `skills/` 和 `references/`）。

**规模**：

```text
laohu-ai-visual      副本 218 / 自有 40  → 85%
laohu-image-creation 副本  91 / 自有 179 → 34%
laohu-script-writer  副本  56 / 自有  79 → 41%
laohu-video-prompt   副本  33 / 自有  99 → 25%
laohu-language-mode  副本   0 / 自有   7
```

**根因**：每个包为了能单独上传、单独运行（项目规范要求"单包独立"），把别的包整棵复制进来。

**三个选项，需要老胡选择**：

| 选项 | 做法 | 后果 |
|---|---|---|
| **A（前一轮建议）** | 只保留被链接真正引用的方法，砍掉未被引用的整棵复制 | 副本大幅缩小，`skills/` 骨架消失 |
| B | 全部删除，跨包需求改成读对方包 | 目录最干净，破坏单包独立 |
| C | 保持现状 | 不动 |

**注意**：前一轮已把 `内置方法/` 从 `<板块>/references/内置方法/` 移到 `<板块>/内置方法/`，老胡认为这个做法不对（"你做成什么内置方法，你不对呀"）。**执行前必须重新确认方案。**

### 7.2 前几轮多建的同级文件（36 个）

图片包 15 个、视频包 12 个、摄影 9 个，散落在各专业 `references/` 下。**清单在** `/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/04_诊断与系统日志/全库整理方案.md` 第二节，35 项逐条列了源文件与去向。

### 7.3 剧本特征资产（854 行）

**源文件**：`/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/references/高质量AI剧本开发计划.md`

**性质**：它是老胡早期做的**审美特征抽象层**。判断它有没有用的方法是：**它里面的话，能不能用现有的 skill/reference 说清楚？能 → 它就没用。**

**处置**：内容做成规则融入对应专业，然后**删除原件**。

```text
0 描述粒度 ＋ 1 不变骨架 ＋ 9 权重取舍 ＋ 10 最终定义
   → /Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/references/剧本特征与判据.md（新建）

2 选题角度 → /Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/skills/laohu-premise-theme/references/创意开发与主题检验.md
3 人物     → /Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/skills/laohu-character-conflict/references/人物行动与连续性.md
4 冲突结构 → /Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/skills/laohu-story-structure/references/结构尺度与高潮设计.md（已并入）
5 场景     → /Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/skills/laohu-scene-craft/references/场面推进与苦难叙事.md
6 台词声音 → /Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/skills/laohu-dialogue/references/本地对白与表演补充.md
7 视听化   → /Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/skills/laohu-format-adaptation/references/镜头化剧本与连续性.md
8 传播包装 → /Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/skills/laohu-industry-business/references/（该专业已有文件）
```

### 7.4 A1 重叠段（尚未扫描）

全库扫描"同一件事在两个文件各写一份"的段落。**这一步必须先做**，否则搬完还会剩重复。

### 7.5 机械四层骨架（101 个 SKILL.md）

`/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/AGENTS.md` 第 3 节明令禁止：

> 禁止为了展示架构，把每个 Skill 机械写成"灵魂层／筋骨层／血肉层／表皮层"。

现状 101/119 个 SKILL.md 在这么做。**骨架本身不承载路由信息，但每次加载都占 token。** 处置：保留有实际功能的，删掉只为凑格式的。

### 7.6 十九个编剧专业：**不动**

位置：`/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/.agents/skills/laohu-script-writer/skills/`（19 个目录）

它们是精准的专业能力，来自外部仓库 `jtydhr88/screenwriting-skills`（提交 `3d2879742919de4f90214dfd703d9b16475331aa`），本地改名加 `laohu-` 前缀。

**已核实且正常**：

```text
上游 20 入口 → 本地 19 专业全部有落点，Reference 零丢失，孤儿 0 个
兄弟专业之间的 12 处链接全部带 laohu- 前缀，全部可达
失效链接 0 处
```

**注意**：这 19 个专业沿用上游的 `reference.md` 布局（根级），与本地其余 100 个专业的 `references/` 布局不一致。**前一轮建议保留上游身份只做标注，老胡尚未答复。**

---

## 八、输出要求

### 8.1 先出方案，再执行

**不要边做边定结构。** 先把整个文件梳理一遍，出一份方案，方案确认后执行只是照做。

### 8.2 方案必须包含

```text
1. 哪个文件跟哪个文件融合（源绝对路径 → 目标绝对路径）
2. 哪个文件移到哪个位置
3. 哪个文件该存在什么位置
4. 哪个文件删除
5. 整理后的完整目录结构
```

**所有路径必须是绝对路径**（见 `/Users/a1/.dsh/AGENTS.md` 第零节）。

### 8.3 最终交付

```text
1. 整理后的真实文件系统
2. 一份完整目录树，列出每个文件，无省略
3. 一份实施记录：改了什么、为什么、验证结果、未完成项
```

---

## 九、验收标准

```text
结构
  五包与既有专业父子关系保持（不搬技能树）
  references/ 下不再出现 skills/
  没有空目录、没有只有一两句话的新文件

内容
  同一件事只在一个文件写一份
  每个文件的原有信息量、信息密度没有下降
  每条规则能归属到四层之一

工具
  路径校验通过（scripts/validate_skill_packages.py）
  受控副本与源一致（scripts/sync_skill_packages.py --check）
  回归测试通过（python3 -m unittest discover -s tests）
  五包分别复制到项目外仍可独立工作

交付
  完整目录树已刷新（scripts/update_skills_readme_tree.py）
  实施记录已写（/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/04_诊断与系统日志/）
```

---

## 十、禁止事项

```text
禁止重写现有内容
      整合是搬运、合并、删除，不是重新创作

禁止把"信息不变"理解成"文字不变"
      编号、语序、衔接可以调；信息量、信息密度、意图不能减

禁止自己发明新的写作规约和模板
      项目已有判断方式（四层能力），用它的

禁止把所有反馈都变成永久规则
      先判断是一次性修改、当前任务规则、项目级规则还是长期偏好

禁止边做边定结构
      先出完整方案，确认后执行

禁止用相对路径或只说文件名
      所有文件必须写绝对路径

禁止用 git add -f 绕过 .gitignore
      作品项目、私人素材、生成媒体不进 Git

禁止为了显得系统化而创建目录和文件
```

---

## 十一、工具与流程

### 11.1 现有脚本（路径均为绝对路径）

```bash
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/scripts/validate_skill_packages.py     # 路径与包独立性校验
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/scripts/sync_skill_packages.py         # 受控副本同步（--check 只检查）
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/scripts/update_skills_readme_tree.py   # 刷新 README 实际文件树
/Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉/scripts/record_method_evolution.py     # 记录被授权修改的已迁移文件
```

### 11.2 每轮改动后的固定流程

```bash
cd /Volumes/Laohu_Work/项目/老胡/老胡自媒体/老胡AI视觉
python3 scripts/validate_skill_packages.py
python3 scripts/sync_skill_packages.py --check
python3 -m unittest discover -s tests
python3 scripts/update_skills_readme_tree.py
```

### 11.3 全局工作规则

执行前必读 `/Users/a1/.dsh/AGENTS.md`（用户级全局指令，含绝对路径规则、第一性判断、反馈吸收、反向审稿、反迎合、文件纪律、表达规则）。

---

## 附：老胡的原话要点（帮助理解意图，不必逐字执行）

```text
「目录层级其实就是关乎着它的调用的这个东西的一个层级的关系」
「我要从目录上面能够看到整个，能够感受到这种清晰的划分规划」
「我又不希望你拆的太多，让这个AI失去对应的注意力」
「如果能够合并起来，加载到一个文件里面，那肯定比加载好多好多文件里面去读取要更方便」
「又不能融合的太多，因为融合的太多的话，它又不是一个分类的，不是一个细节」
「我是以模块的这种功能、这种复用性去进行组合拆分的，这个模块它又不能太小」
「如果A跟B它天然就应该融合在一起，它就不应该拆成A跟B」
「你要识别的是意图，他的目的，他的要求，他的需求，然后去进行对应的映射」
「Reference文件应该是这个东西正确应该怎么做，它是一个正面的描述」
「SKILL.md不仅仅是判断什么意图该定谁该找谁，它本身也有对应的这种功能」
「不是让你文字不变，我说的是信息量、信息密度和原本的意思不变」
「你先把它梳理一遍，然后直接给一个方案，变成只要去执行就行了」
「所有涉及到目录的文件的东西，你都要用绝对路径给我指出来」
```
