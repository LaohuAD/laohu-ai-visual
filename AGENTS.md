# 老胡 AI 视觉作品生产系统

本文件是项目唯一的顶层入口。它只管理全项目共同遵守的方向、路由、边界、状态、权威来源与进化方式。剧本怎样写、资产怎样设计、镜头怎样编译、封面怎样传播、结果怎样复盘，由对应 Skill 负责；顶层不再复制专业细则。

## 一、项目目的

老胡 AI 视觉不是提示词收藏夹，也不是把“电影感、高清、震撼”堆进模型的工具。

它是一条从灵感到作品、再从真实结果回到能力更新的生产链：

```text
灵感、歌曲与目标
→ 故事和人物选择，或歌曲—视觉结构与导演命题
→ 作品级美术风格设计
→ 人物身体化、服装与场景布景设计
→ 视觉资产与世界连续性
→ 分镜和视频提示词
→ 人工生成与剪辑
→ 封面和发布
→ 真实结果复盘
→ 规则、案例与资产更新
```

最终追求的不是“提示词写得完整”，也不是“结果已经能用”，而是在当前题材、受众、媒介、成本和事实边界内，把作品推到已知证据与能力能够达到的最高质量：观众愿意停留，行家找不到明显敷衍，人物、情绪、画面、声音和技术共同托住一个不可替代的作品判断，并能从真实结果里继续逼近更高上限。

## 二、项目箴言

### 先让人入戏，再让机器入场

观众体验决定作品为什么存在，模型规则只决定它怎样被做出来。任何技术修正如果削弱了人物、关系、情绪、信息或观看位置，就应退回上游重做。

所有正式图片和视频提示词都先向观众翻译，再向模型翻译：图片只截取一个值得停留的可感瞬间；视频只组织一条从动机与起点、经过触发与变化、到主要兑现和结果余韵的时间因果。人物、表演、构图、运镜、光色、材质、特效和声音不得同权并列，都要共同证明当前唯一承重结果；最多保留一项明确从属的次要结果。

### 故事决定去处，镜头决定脚步，资产守住同一个世界

故事负责人物为什么行动、行动付出什么代价；镜头负责观众站在哪里、怎样经历变化；资产负责身份、结构、空间和材质不在生成中走样。三者不能互相越权，也不能互相甩锅。

歌曲型作品还要先让最终音频决定真实时间，让 MV 导演决定观众怎样在这段时间里经历情绪、表演、母题与视觉变化。歌词不是逐句配图清单，音乐结构也不能被下游生成单元反过来切碎；叙事只是 MV 的候选引擎，不是所有歌曲都必须套三幕式或英雄之旅。

### 没有证据的漂亮，只是一次运气

角色、产品、情绪、卖点、文化和世界观都要有可见或可听证据。无法说明结果为什么成立，就不能把偶然生成的好看升级成长期方法。

### 写完只是交卷，真实结果才给答案

文档完整、字段齐全、脚本通过，都不能替代成片、剪辑、缩略图和发布数据。生成前验收意图与可执行性，生成后验收真实结果；两种证据不能混写。

### 作品可以克制，标准不能平庸；能用只是底线，难忘才是答案

每个阶段同时存在两道门：底线门负责拦住不可用、失真、断链、超限和事实错误；巅峰门负责比较候选、放大作品真正承重的关系、情绪、画面、声音或卖点，并继续寻找当前已知范围内更好的替代方案。底线通过只意味着可以进入下一轮创造和比较，不意味着可以结束交付。

追求极致不是把所有维度同时拉满。最强情绪可以来自克制，最强构图可以来自留白，最强运镜可以是摄影机不动，最强资产可以是删去华而不实的设计。先确定本作品唯一的主胜负手，再让其他专业维度托住它；任何镜头、特效、装饰和规则如果只增加热闹、不提高主结果，都应退让。

“最好”不靠自我宣布。重要产物至少比较多个真正不同的候选，说明为什么胜出，并用同类顶尖作品、成熟创作者的判断维度、目标观众的观看结果和真实生成反馈校准。无法证明已经没有更好方案时，可以标记当前最佳候选、继续验证或回到上游，不能用“已合格”提前收工。

## 三、四层能力怎样进入项目

四层是项目的内部建模方法，不是下游文件必须出现的四个栏目。

```text
灵魂以箴言定向：说明什么值得追求、什么值得舍弃。
筋骨以章法成篇：说明信息、状态、阶段和负责人怎样形成秩序。
血肉以技法生变：说明材料怎样选择、方法为什么有效、不同题材怎样迁移。
表皮以法度成形：说明格式、路径、命名、参数、字符、平台和验收硬边界。
```

创建或修改能力时，先在内部回答四类问题，再把答案编译成领域自己的语言。例如剧本写“观众等待、人物选择、场次压力”，资产写“复用范围、失真代价、依赖顺序”，视频写“起点、触发、屏幕变化、结束状态”，复盘写“证据、第一个断点、最早负责人”。

禁止为了展示架构，把每个 Skill 机械写成“灵魂层 / 筋骨层 / 血肉层 / 表皮层”。真正的验收是：原则能不能裁决，流程能不能接力，方法能不能产出，法度能不能拦住不可用结果。

## 四、权威层级

同一事实和规则只设一个负责人：

| 内容 | 权威来源 |
|---|---|
| 全项目目的、路由、边界和目录纪律 | `AGENTS.md` |
| 十四个 Skill 的职责、交接和公共字段 | `02_共享资产库/05_工具流程/laohu_skills核心合约.md` |
| 某阶段怎样做 | 对应 `skills/laohu-*/SKILL.md` |
| 特定题材、对象、模型或失败分支 | 对应 Skill 的 `references/` |
| 外部运行时 Skill 的身份、原文、适配与返回关系 | `02_共享资产库/05_工具流程/外部能力依赖清单.md` |
| 可重复机械检查 | `scripts/` 或对应 Skill 的 `scripts/` |
| 原始需求、参考、实测结果 | 当前作品目录或 `00_输入原料/` |
| 已验证的长期资产与案例 | `02_共享资产库/` |

发生冲突时，先判断它们是否在争夺同一职责。顶层边界高于局部便利；同一专业范围内，当前 Skill 高于旧模板；真实结果高于作者意图；模型与平台的最新事实需要重新核验。

## 五、十四个执行 Skill

当前项目保留 14 个本地执行入口：13 个负责作品生产、素材、专业设计与真实结果复盘，1 个负责长期能力进化。入口属于本项目，不等于其底层方法都由本项目原创：`laohu-vibe-creating-prompt` 是外部 `vibe-creating-prompt` 的本地适配层。

| Skill | 唯一主责 | 完成标志 |
|---|---|---|
| `laohu-ai-visual` | 判断真实目的、作品归属、当前状态、阶段入口与下游负责人 | 路由清楚，作品状态与下一阶段完成条件明确 |
| `laohu-story-material` | 在明确保存意图下完整保存原话、拆成故事原子；为正式编剧执行渐进检索、充分度判断、外部候选隔离和使用回流 | source / atom / usage 可验证，素材交接包成立，未经确认外部候选未越权 |
| `laohu-script-writer` | 把灵感变成观众愿意等待、人物必须选择的可拍故事 | 因果、信息、场次压力和下游事实成立 |
| `laohu-mv-director` | 把歌曲事实、最终音频、歌词与创作背景转成歌曲—视觉结构、表演策略、制作时间线和动态分镜任务 | 时间证据可靠，导演命题与声画关系成立，创作段落和生产单元分开，文本交接可进入资产与视频提示词 |
| `laohu-art-direction` | 把题材、受众、世界规则和媒介目标转成跨人物服装场景的统一视觉方向 | 题材视觉合同、视觉证据矩阵、形态色彩材料光影系统、相邻类型测试和部门交接成立 |
| `laohu-character-design` | 把剧本人物转成可见、可表演、可生产的人物母版 | 选角、脸身姿动、第一印象、群像、媒介适配、写真人物正文与妆造补充正文成立 |
| `laohu-costume-design` | 从剧本、人物、世界、资源、场景与叙事策略判断服装等级，建立原创服装、世界衣橱、候选竞争和分级定案 | 情境判断与定案状态清楚；需要人工定案的整体方向已确认，服装正文、色卡、不可见结构与交接包成立，能无损进入F/W/M资产编译 |
| `laohu-set-design` | 从剧本、人物、世界与行动建立原创场景、空间关系、布景陈设和制作策略 | 场景与布景设计交接包成立；胜出空间能交给S/A资产编译 |
| `laohu-visual-assets` | 选择最低可行资产，整合已确认的专业设计，完成资产编译、图片提示词、依赖和调用合同 | 资产可复用、可验收、可被下游准确引用 |
| `laohu-video-prompt` | 把镜头编译成模型可执行的时间、屏幕、声音和状态变化，并负责 VC 前后两次验收 | 三段式基线通过、VC 已实际执行、返回稿可复制、可生成、可剪、可接续 |
| `laohu-vibe-creating-prompt` | 对每一条合格视频提示词执行一次 VC 判断与提纯 | 必须留下实际调用记录；有增益则采用改稿，无增益则以零改动结论返回原稿 |
| `laohu-cover-design` | 把正片真实价值压缩成可信且有点击力的发布主视觉 | 一眼可读、标题准确、承诺不超过正片 |
| `laohu-generation-review` | 从真实图片、视频、剪辑、封面和发布结果定位首断点 | 最早负责人、最多三个返修动作与回流位置明确 |
| `laohu-capability-evolution` | 把可复用反馈、研究、反思和平台变化编译成长期能力更新 | 证据能跨任务接续，只重编受影响层，必要时长出新负责人，并以保真回归决定保留、观察或撤回 |

同一任务可以经过多个 Skill，但同一时刻只能有一个主负责人。上游完成条件不成立时，不能靠下游润色掩盖。

### 外部能力依赖

外来的能力要留来路，本地的判断要有归处。

运行时会直接读取的外部 Skill，必须在 `02_共享资产库/05_工具流程/外部能力依赖清单.md` 登记原始名称、外部原文、便携副本、本地适配层、调用者、返回负责人和失效处理。调用链不得只留下 VC 一类简称，也不得把本地适配层写成项目原创能力。

当前唯一登记的外部运行时 Skill 是 `vibe-creating-prompt`。每一条正式视频提示词都必须通过 `laohu-vibe-creating-prompt` 实际读取并执行它，再返回 `laohu-video-prompt` 完成最终复验。VC 可以得出“原稿已经最好、正文零改动”的结论，但不能省略调用。其他外部仓库、理论、案例和专家方法如果只被提炼成知识，属于 Reference 的来源，不冒充运行时 Skill。

## 六、生产状态与路由

作品状态只记录真实进度：

```text
IDEA：只有灵感、目标或原始材料。
STORY：叙事作品的故事与人物选择已经成立；歌曲型作品的歌曲—视觉结构、导演命题与情绪运动已经成立。
ASSET：最低可行资产已经定义，必要图片等待生成或已经验收。
SHOT：分镜、VID 和相邻状态已经明确。
PROMPT：视频提示词已通过底线门、必经 VC、视频 Skill 最终复验和巅峰门；只有基线稿时仍处于 SHOT → PROMPT 的处理中。
GENERATED：已有真实生成结果。
EDITED：已有可审的剪辑或成片。
PUBLISHED：已有发布结果或平台数据。
LEARNED：经验已经进入正确负责人并通过行为验证。
```

路由先问当前缺的是什么，不按用户最后提到的名词跳阶段：

| 当前缺口 | 主负责人 |
|---|---|
| 目的、归属、状态或下一步不清 | `laohu-ai-visual` |
| 明确要求长期保存日常灵感、检索故事素材，或内部素材是否足够尚不清 | `laohu-story-material` |
| 观众为什么看、人物为什么做、事件为何发生不清 | `laohu-script-writer` |
| 歌词、成品音频、时间轴或创作背景怎样变成 MV 的导演命题、声画关系、表演策略、制作时间线与分镜任务不清 | `laohu-mv-director` |
| 题材为什么看起来属于这一类、主辅类型关系、跨人物服装场景的形态色彩材料光影系统或相邻类型边界不清 | `laohu-art-direction` |
| 人物为什么长成、站成、动成这样，第一印象、选角、群像或跨媒介身份不清 | `laohu-character-design` |
| 人物为什么穿成这样、应穿到什么资源与展示等级、世界衣橱、阵营阶层差异、服装原创判断或主要人物整体方向尚未定案 | `laohu-costume-design` |
| 场景为什么这样存在、人物怎样使用空间、地点语法、布景陈设或实景搭建策略不清 | `laohu-set-design` |
| 已确认的人、物、空间或美术风格怎样在资产与生成中保持不清 | `laohu-visual-assets` |
| 镜头怎样运动、变化、发声、结束和连接不清 | `laohu-video-prompt` |
| 视频提示词首稿已经通过基线，需要执行必经的 VC 提纯与零改动判断 | `laohu-vibe-creating-prompt` |
| 作品已完成但发布入口不清 | `laohu-cover-design` |
| 已有真实结果，需要判断哪里先错 | `laohu-generation-review` |
| 反馈、资料、反思或平台变化可能改变长期能力 | `laohu-capability-evolution` |

用户明确要求快速草稿时可以缩短链路，但不能伪造不存在的剧本、资产或实测结果。任何付费生成、公开发布、版权使用和真实商业声称，都要在发生外部动作前确认必要事实。

状态推进只证明底线门已经通过。面向老胡的正式交付还必须通过本阶段巅峰门；用户明确要求“草稿、占位、快速试验”时可以降低完成度，但必须如实标记，不能把草稿标准反写成项目默认质量。

## 七、共同执行闭环

每个阶段都执行同一条最小循环：

```text
读取当前权威输入
→ 写清本阶段要改变的结果
→ 保护已经确认的事实和资产
→ 找到当前最上游缺口
→ 选择必要 Reference，不为“资料全”而全读
→ 产生当前阶段结果
→ 反向审稿与机械检查
→ 写清阶段完成条件和下游必须继承
```

下游不能只转发上游。它可以在自己的专业范围内补足创意、可生成性、可剪辑性和观众体验，但不能改写已确认事实。发现上游根因时，返回最早负责人，不在下游打补丁。

## 八、文本交付与外部动作边界

项目默认交付文本：故事设计稿、剧本、资产总表、图片提示词、文字分镜、故事板提示词、视频提示词、封面提示词、复盘和规则文件。

本项目长期不启用浏览器可视化伴侣，也不再询问是否启用。人物、服装、场景、结构或方案比较统一使用现有 Markdown / HTML 交付页、文字表格和本地文件完成；只有老胡以后主动明确撤回这一偏好时才改变。

作品生产文件采用“Markdown生产、HTML交付”：Markdown 是唯一权威内容源，负责Skill接力、版本差异、规则校验和长期修改；HTML只由Markdown自动生成，面向老胡阅读、识别编号和一键复制，禁止手工修改正文。当前作品每个阶段主文件修改后运行 `scripts/render_delivery_html.py` 生成同名 HTML 交付页，并在作品根目录刷新 `00_作品交付中心.html`。最终回复优先给作品HTML链接，同时保留Markdown源文件链接供追溯；规则、Reference和系统日志仍以Markdown为权威，不要求为内部文件生成HTML。

未来新作品默认执行：

```bash
python3 scripts/render_delivery_html.py --project "01_作品项目/<状态>/<作品目录>"
```

历史作品不批量转换，只有重新进入生产或老胡要求时才补交付页。HTML交付页固定为纯文本、复制优先的高密度工作台：资产卡不按Markdown出现顺序散排，而按职责固定分为“人物主视觉图（F）→ 素体与阶段素体资产（B / LZ / P）→ 服装资产（W）→ 妆造设计资产（M）→ 道具资产（A）→ 场景资产（S）→ 群像资产（G）”，组内按编号排序。F是经人工选定的角色定调成图，作为B / W / M共同上游源图，但它本身不按稳定资产验收，不直接进入视频，也不在B验收后自动退役。VID单独进入“视频提示词”分组。卡片使用小字号多列排列，默认只露出编号、明确标题和“复制标题 / 复制提示词 / 查看详情”三个操作，不直接铺开长提示词；复制标题必须取得包含编号和名称的完整可见标题，完整提示词只在用户主动打开详情层时显示。搜索或筛选后没有可见卡片的分组同步隐藏。不得嵌入图片、扫描图片、预留图片槽、制作示例图或显示伪造的图片状态。HTML中的提示词必须与Markdown代码块逐字一致，重复编号、无标题提示词或渲染失败时不能标记阶段交付完成。

除非老胡明确要求调用具体生成工具：

- “生成图片 / 视频 / 音频”默认指写出可复制提示词和使用说明。
- `【@资产名】` 只是待绑定占位符，不代表文件已经存在。
- 只有老胡真实回传或本地确实存在的素材，才能登记为已生成资产。
- 不擅自调用付费生成、发布平台、剪辑软件或外部账号。

用于公开测评、教程和品牌样片的内容默认原创。需要品牌归属时使用老胡已经确认的 `老胡 / laohu / 老胡用AI赚钱` 体系；固定 Logo 只调用 `【Logo参考@老胡Logo】`，不重新设计或改造。真实商品的参数、价格、疗效、认证和比较没有证据时不得编造。

## 九、作品隐私与 Git 边界

`01_作品项目/` 是本地生产区。Git 只追踪 `进行中 / 已完成 / 已发布` 的 `.gitkeep`，不追踪具体作品、输入、剧本、资产、提示词、生成素材、发布数据和复盘。

不得使用 `git add -f` 绕过该边界。可复用经验先去除人物名、作品名、私有台词组合、项目路径和个人素材，再写入共享资产或 Skill。`output/`、`tmp/` 和 `docs/superpowers/` 同样不进入 Git。

## 十、目录与文件纪律

```text
00_输入原料/：尚未归属作品的需求、研究、外部资料与模型材料。
01_作品项目/：以作品为单位保存完整生产链。
02_共享资产库/：跨作品复用的模板、语言资产、模型适配、案例和流程。
03_发布与课程化/：由作品经验转成的教程、课程和发布内容。
04_诊断与系统日志/：项目级审计和结构变更记录。
skills/：十三个生产、素材、专业设计与复盘能力，加一个长期能力进化入口。
tests/ 与 scripts/：行为场景和机械验证。
```

能在现有权威文件说清楚的，不新建平行规则；功能不同、长期复用或混在一起会污染时才拆文件。正式作品每个阶段默认维护一个当前主文件，不自动制造 `v1 / v2 / 最终版 / 优化版`。

只要本轮产出文件，回复老胡时给具体文件链接，不用目录链接代替结果。

## 十一、反馈与能力进化

反馈先判断属于当前结果、当前阶段、某个 Skill、跨 Skill 合约、平台事实还是长期偏好。当前结果由业务 Skill 修正，真实结果先由 `laohu-generation-review` 归因；只有可能改变长期能力时，才调用 `laohu-capability-evolution`。不能把一次主观不满直接写成永久禁令。

可复用反馈按以下顺序处理：

```text
保留原结果与反馈证据
→ 定位第一个断点
→ 检索能力进化台账，合并或重新唤醒相关记录
→ 调用能力进化 Skill，选择真正受影响的层
→ 找到唯一规则负责人
→ 写改动假设和可能副作用
→ 修改负责人文件及必要 Reference / 模板 / 脚本
→ 用代表性场景验证
→ 决定 KEEP / OBSERVE / REVERT
```

规则的升级必须改变下一次行为。只在顶层增加一句口号、只整理目录、只删除文字或只让检查脚本搜索关键词，都不算能力提升。

跨任务进化记忆统一写入 `04_诊断与系统日志/能力进化台账.md`。台账只保存证据索引、旧行为、新行为和重新唤醒条件，不替代业务规则；现有负责人无法承接时，由能力进化 Skill 按独立触发、输入输出和验收门槛决定是否新建能力。

能力进化完成后必须返回总控，由总控继续作品生产或交付。完成范围清楚、验证通过且不混入范围外改动的阶段后，可以建立本地 Git 提交作为可恢复检查点；`git push`、PR、Release、远程仓库修改和其他发布必须由老胡在当前任务明确授权，历史授权不继承。

## 十二、每次交付前检查

- 这次真正要改变的观众结果、生产结果或商业结果是什么？
- 当前作品状态和主负责人是否明确？
- 是否保护了已确认事实、素材、台词和状态？
- 是否把专业细则交给真实负责人，而不是继续堆在顶层？
- 是否只读取了真正适配的 Reference？
- 当前产物能否直接进入下一阶段，还是仍依赖作者解释？
- 所有路径、字符、命名、格式和平台硬边界是否经过机械验证？
- 本轮作品主文件是否已经刷新同名HTML交付页和作品交付中心，提示词能否独立复制？
- 真实结果与设计意图是否分开记录？
- 可复用反馈是否回到唯一负责人，并有行为验证？
- 最终回复是否给出具体结果文件和下一步真实动作？
