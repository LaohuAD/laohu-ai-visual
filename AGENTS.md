# 老胡 AI 视觉作品生产系统

本文件是项目唯一的顶层入口。它只管理全项目共同遵守的方向、路由、边界、状态、权威来源与进化方式。剧本怎样写、资产怎样设计、镜头怎样编译、封面怎样传播、结果怎样复盘，由对应 Skill 负责；顶层不再复制专业细则。

## 一、项目目的

老胡 AI 视觉不是提示词收藏夹，也不是把“电影感、高清、震撼”堆进模型的工具。

它是一条从灵感到作品、再从真实结果回到能力更新的生产链：

```text
灵感与目标
→ 故事和人物选择
→ 视觉资产与世界连续性
→ 分镜和视频提示词
→ 人工生成与剪辑
→ 封面和发布
→ 真实结果复盘
→ 规则、案例与资产更新
```

最终追求的不是“提示词写得完整”，而是作品能让观众进入、能被模型执行、能被后期使用，并能从一次次真实结果里长出更稳的能力。

## 二、项目箴言

### 先让人入戏，再让机器入场

观众体验决定作品为什么存在，模型规则只决定它怎样被做出来。任何技术修正如果削弱了人物、关系、情绪、信息或观看位置，就应退回上游重做。

### 故事决定去处，镜头决定脚步，资产守住同一个世界

故事负责人物为什么行动、行动付出什么代价；镜头负责观众站在哪里、怎样经历变化；资产负责身份、结构、空间和材质不在生成中走样。三者不能互相越权，也不能互相甩锅。

### 没有证据的漂亮，只是一次运气

角色、产品、情绪、卖点、文化和世界观都要有可见或可听证据。无法说明结果为什么成立，就不能把偶然生成的好看升级成长期方法。

### 写完只是交卷，真实结果才给答案

文档完整、字段齐全、脚本通过，都不能替代成片、剪辑、缩略图和发布数据。生成前验收意图与可执行性，生成后验收真实结果；两种证据不能混写。

## 三、四层能力怎样进入项目

四层是项目的内部建模方法，不是下游文件必须出现的四个栏目。

```text
灵魂以箴言定向：说明什么值得追求、什么值得舍弃。
筋骨以章法成篇：说明信息、状态、阶段和负责人怎样形成秩序。
血肉以技法生变：说明材料怎样选择、方法为什么有效、不同题材怎样迁移。
表皮以法度成形：说明格式、路径、命名、参数、字符、平台和验收硬边界。
```

创建或修改能力时，先在内部回答四类问题，再把答案编译成领域自己的语言。例如剧本写“观众等待、人物选择、场次压力”，资产写“复用范围、失真代价、依赖顺序”，视频写“起点、触发、屏幕变化、结束状态”，复盘写“证据、第一个断点、最早负责人”。

禁止为了展示架构，把每个 Skill 机械写成“灵魂层 / 筋骨层 / 血肉层 / 表皮层”。真正的验收是：原则能不能裁决，流程能不能接力，方法能不能产出，法度能不能拦住不可用结果。

## 四、权威层级

同一事实和规则只设一个负责人：

| 内容 | 权威来源 |
|---|---|
| 全项目目的、路由、边界和目录纪律 | `AGENTS.md` |
| 八个 Skill 的职责、交接和公共字段 | `02_共享资产库/05_工具流程/laohu_skills核心合约.md` |
| 某阶段怎样做 | 对应 `skills/laohu-*/SKILL.md` |
| 特定题材、对象、模型或失败分支 | 对应 Skill 的 `references/` |
| 外部运行时 Skill 的身份、原文、适配与返回关系 | `02_共享资产库/05_工具流程/外部能力依赖清单.md` |
| 可重复机械检查 | `scripts/` 或对应 Skill 的 `scripts/` |
| 原始需求、参考、实测结果 | 当前作品目录或 `00_输入原料/` |
| 已验证的长期资产与案例 | `02_共享资产库/` |

发生冲突时，先判断它们是否在争夺同一职责。顶层边界高于局部便利；同一专业范围内，当前 Skill 高于旧模板；真实结果高于作者意图；模型与平台的最新事实需要重新核验。

## 五、八个执行 Skill

当前项目保留 8 个本地执行入口：7 个负责作品生产与真实结果复盘，1 个负责长期能力进化。入口属于本项目，不等于其底层方法都由本项目原创：`laohu-vibe-creating-prompt` 是外部 `vibe-creating-prompt` 的本地适配层。

| Skill | 唯一主责 | 完成标志 |
|---|---|---|
| `laohu-ai-visual` | 判断真实目的、作品归属、当前状态、阶段入口与下游负责人 | 路由清楚，作品状态与下一阶段完成条件明确 |
| `laohu-script-writer` | 把灵感变成观众愿意等待、人物必须选择的可拍故事 | 因果、信息、场次压力和下游事实成立 |
| `laohu-visual-assets` | 选择最低可行资产，完成美术设计、依赖和调用合同 | 资产可复用、可验收、可被下游准确引用 |
| `laohu-video-prompt` | 把镜头编译成模型可执行的时间、屏幕、声音和状态变化 | 三段式正文可复制、可生成、可剪、可接续 |
| `laohu-vibe-creating-prompt` | 在合格首稿上增强观众可感知的作品身份和峰值 | 至少一个可感知维度变强；无增益则保留原稿 |
| `laohu-cover-design` | 把正片真实价值压缩成可信且有点击力的发布主视觉 | 一眼可读、标题准确、承诺不超过正片 |
| `laohu-generation-review` | 从真实图片、视频、剪辑、封面和发布结果定位首断点 | 最早负责人、最多三个返修动作与回流位置明确 |
| `laohu-capability-evolution` | 把可复用反馈、研究、反思和平台变化编译成长期能力更新 | 证据能跨任务接续，只重编受影响层，必要时长出新负责人，并以保真回归决定保留、观察或撤回 |

同一任务可以经过多个 Skill，但同一时刻只能有一个主负责人。上游完成条件不成立时，不能靠下游润色掩盖。

### 外部能力依赖

外来的能力要留来路，本地的判断要有归处。

运行时会直接读取的外部 Skill，必须在 `02_共享资产库/05_工具流程/外部能力依赖清单.md` 登记原始名称、外部原文、便携副本、本地适配层、调用者、返回负责人和失效处理。调用链不得只留下 VC 一类简称，也不得把本地适配层写成项目原创能力。

当前唯一登记的外部运行时 Skill 是 `vibe-creating-prompt`。项目通过 `laohu-vibe-creating-prompt` 调用它，结果必须返回 `laohu-video-prompt` 完成最终复验。其他外部仓库、理论、案例和专家方法如果只被提炼成知识，属于 Reference 的来源，不冒充运行时 Skill。

## 六、生产状态与路由

作品状态只记录真实进度：

```text
IDEA：只有灵感、目标或原始材料。
STORY：故事与人物选择已经成立。
ASSET：最低可行资产已经定义，必要图片等待生成或已经验收。
SHOT：分镜、VID 和相邻状态已经明确。
PROMPT：正式视频提示词已通过生成前验收。
GENERATED：已有真实生成结果。
EDITED：已有可审的剪辑或成片。
PUBLISHED：已有发布结果或平台数据。
LEARNED：经验已经进入正确负责人并通过行为验证。
```

路由先问当前缺的是什么，不按用户最后提到的名词跳阶段：

| 当前缺口 | 主负责人 |
|---|---|
| 目的、归属、状态或下一步不清 | `laohu-ai-visual` |
| 观众为什么看、人物为什么做、事件为何发生不清 | `laohu-script-writer` |
| 同一个人、物、空间或风格怎样保持不清 | `laohu-visual-assets` |
| 镜头怎样运动、变化、发声、结束和连接不清 | `laohu-video-prompt` |
| 合格提示词仍普通、峰值不清、作品身份不足 | `laohu-vibe-creating-prompt` |
| 作品已完成但发布入口不清 | `laohu-cover-design` |
| 已有真实结果，需要判断哪里先错 | `laohu-generation-review` |
| 反馈、资料、反思或平台变化可能改变长期能力 | `laohu-capability-evolution` |

用户明确要求快速草稿时可以缩短链路，但不能伪造不存在的剧本、资产或实测结果。任何付费生成、公开发布、版权使用和真实商业声称，都要在发生外部动作前确认必要事实。

## 七、共同执行闭环

每个阶段都执行同一条最小循环：

```text
读取当前权威输入
→ 写清本阶段要改变的结果
→ 保护已经确认的事实和资产
→ 找到当前最上游缺口
→ 选择必要 Reference，不为“资料全”而全读
→ 产生当前阶段结果
→ 反向审稿与机械检查
→ 写清阶段完成条件和下游必须继承
```

下游不能只转发上游。它可以在自己的专业范围内补足创意、可生成性、可剪辑性和观众体验，但不能改写已确认事实。发现上游根因时，返回最早负责人，不在下游打补丁。

## 八、文本交付与外部动作边界

项目默认交付文本：故事设计稿、剧本、资产总表、图片提示词、文字分镜、故事板提示词、视频提示词、封面提示词、复盘和规则文件。

除非老胡明确要求调用具体生成工具：

- “生成图片 / 视频 / 音频”默认指写出可复制提示词和使用说明。
- `【@资产名】` 只是待绑定占位符，不代表文件已经存在。
- 只有老胡真实回传或本地确实存在的素材，才能登记为已生成资产。
- 不擅自调用付费生成、发布平台、剪辑软件或外部账号。

用于公开测评、教程和品牌样片的内容默认原创。需要品牌归属时使用老胡已经确认的 `老胡 / laohu / 老胡用AI赚钱` 体系；固定 Logo 只调用 `【Logo参考@老胡Logo】`，不重新设计或改造。真实商品的参数、价格、疗效、认证和比较没有证据时不得编造。

## 九、作品隐私与 Git 边界

`01_作品项目/` 是本地生产区。Git 只追踪 `进行中 / 已完成 / 已发布` 的 `.gitkeep`，不追踪具体作品、输入、剧本、资产、提示词、生成素材、发布数据和复盘。

不得使用 `git add -f` 绕过该边界。可复用经验先去除人物名、作品名、私有台词组合、项目路径和个人素材，再写入共享资产或 Skill。`output/`、`tmp/` 和 `docs/superpowers/` 同样不进入 Git。

## 十、目录与文件纪律

```text
00_输入原料/：尚未归属作品的需求、研究、外部资料与模型材料。
01_作品项目/：以作品为单位保存完整生产链。
02_共享资产库/：跨作品复用的模板、语言资产、模型适配、案例和流程。
03_发布与课程化/：由作品经验转成的教程、课程和发布内容。
04_诊断与系统日志/：项目级审计和结构变更记录。
skills/：七个生产与复盘能力，加一个长期能力进化入口。
tests/ 与 scripts/：行为场景和机械验证。
```

能在现有权威文件说清楚的，不新建平行规则；功能不同、长期复用或混在一起会污染时才拆文件。正式作品每个阶段默认维护一个当前主文件，不自动制造 `v1 / v2 / 最终版 / 优化版`。

只要本轮产出文件，回复老胡时给具体文件链接，不用目录链接代替结果。

## 十一、反馈与能力进化

反馈先判断属于当前结果、当前阶段、某个 Skill、跨 Skill 合约、平台事实还是长期偏好。当前结果由业务 Skill 修正，真实结果先由 `laohu-generation-review` 归因；只有可能改变长期能力时，才调用 `laohu-capability-evolution`。不能把一次主观不满直接写成永久禁令。

可复用反馈按以下顺序处理：

```text
保留原结果与反馈证据
→ 定位第一个断点
→ 检索能力进化台账，合并或重新唤醒相关记录
→ 调用能力进化 Skill，选择真正受影响的层
→ 找到唯一规则负责人
→ 写改动假设和可能副作用
→ 修改负责人文件及必要 Reference / 模板 / 脚本
→ 用代表性场景验证
→ 决定 KEEP / OBSERVE / REVERT
```

规则的升级必须改变下一次行为。只在顶层增加一句口号、只整理目录、只删除文字或只让检查脚本搜索关键词，都不算能力提升。

跨任务进化记忆统一写入 `04_诊断与系统日志/能力进化台账.md`。台账只保存证据索引、旧行为、新行为和重新唤醒条件，不替代业务规则；现有负责人无法承接时，由能力进化 Skill 按独立触发、输入输出和验收门槛决定是否新建能力。

能力进化完成后必须返回总控，由总控继续作品生产或交付。完成范围清楚、验证通过且不混入范围外改动的阶段后，可以建立本地 Git 提交作为可恢复检查点；`git push`、PR、Release、远程仓库修改和其他发布必须由老胡在当前任务明确授权，历史授权不继承。

## 十二、每次交付前检查

- 这次真正要改变的观众结果、生产结果或商业结果是什么？
- 当前作品状态和主负责人是否明确？
- 是否保护了已确认事实、素材、台词和状态？
- 是否把专业细则交给真实负责人，而不是继续堆在顶层？
- 是否只读取了真正适配的 Reference？
- 当前产物能否直接进入下一阶段，还是仍依赖作者解释？
- 所有路径、字符、命名、格式和平台硬边界是否经过机械验证？
- 真实结果与设计意图是否分开记录？
- 可复用反馈是否回到唯一负责人，并有行为验证？
- 最终回复是否给出具体结果文件和下一步真实动作？
