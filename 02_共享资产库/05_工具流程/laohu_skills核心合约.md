# laohu Skills 核心合约

本文件只规定七个 Skill 怎样接力、怎样返回和怎样验收。专业方法由各 Skill 负责；作品事实由当前作品负责；平台机械限制由脚本和模型适配负责。

## 一、总合约

### 各守一段，才能同走一程

上游交付事实、状态和创意空间，下游在自己的专业范围内主动升级。下游不能改写已确认事实，也不能因上游普通就继续产出普通；发现根因不在本阶段时，返回最早负责人。

### 交接不是复述，是状态移交

每次交接至少说明：从什么可靠输入开始、本阶段改变了什么、什么已经锁定、什么仍可设计、怎样判断本阶段完成、下一站从哪里继续。

### 一处失效，回到最早断点

剧本因果不成立，不靠资产美术补；资产失真，不靠视频长文补；镜头编译错误，不靠 VC 润色补；正片承诺不足，不靠封面夸大补。

## 二、七个负责人

| Skill | 入口 | 本阶段必须完成 | 下游 |
|---|---|---|---|
| `laohu-ai-visual` | 任意输入、混合需求、项目级反馈 | 真实结果、作品状态、阶段入口、主负责人、完成条件 | 任一对应 Skill |
| `laohu-script-writer` | 灵感、口述、商业目标、故事或剧本 | 观众等待、人物选择、事件因果、场次结果、下一场压力 | 资产 / 视频 |
| `laohu-visual-assets` | 已确认故事、镜头需求、身份与连续性问题 | 最低可行资产、设计方向、依赖顺序、图片提示词、验收和调用 | 视频 / 封面 / 复盘 |
| `laohu-video-prompt` | 已确认镜头与必要资产 | 起点、触发、屏幕变化、声画结果、结束状态、三段式和字符实测 | VC / 人工生成 / 复盘 |
| `laohu-vibe-creating-prompt` | 已通过基线的正式视频提示词 | 至少一个观众可感知增益；无增益则保留原稿 | 视频 Skill 回填 |
| `laohu-cover-design` | 已完成作品、最终提示词、成片与发布定位 | 点击承诺、第一落点、逐字标题、参考职责和比例适配 | 人工生图 / 复盘 |
| `laohu-generation-review` | 真实生成、剪辑、封面、发布数据或反馈 | 第一个断点、最早负责人、最多三个返修动作、经验回流 | 对应上游 / 总控 |

## 三、VC 身份与调用链

VC 不是第八个本地原创方法，而是外部 Skill `vibe-creating-prompt` 的项目简称。当前生产链不得直接读取外部原文执行，而要经过本地适配入口 `laohu-vibe-creating-prompt`：

```text
laohu-video-prompt 完成可直接生成的首稿
→ laohu-vibe-creating-prompt 判断是否调用外部 VC
→ 按需读取外部原文的便携副本
→ 本地适配层保护台词、资产、状态、三段式、字符和剪辑接口
→ 结果返回 laohu-video-prompt
→ 视频 Skill 完成最终复验与写入
```

外部原文位于 `02_共享资产库/05_工具流程/外部优化Skill/vibe-creating-prompt/SKILL.md`，便携副本位于 `skills/laohu-vibe-creating-prompt/references/01_外部Vibe_Creating原文.md`，两者必须逐字一致。身份、来源状态、更新和失效处理以 `外部能力依赖清单.md` 为准。

任一原文缺失、镜像漂移或适配关系不完整时，保留已经通过视频首稿门的基线，跳过 VC 并返回 `laohu-video-prompt`；不得凭简称猜测能力，也不得让外部增强环节破坏基础产物。

## 四、公共任务头

非机械任务在内部保留：

```text
作品名与目录：
作品状态：
本轮真实结果：
目标观众与平台：
阶段入口与权威输入：
上游已确认事实：
上游尚未解决的缺口：
本阶段允许新增的创意：
本阶段完成条件：
输出位置：
下游负责人：
下游必须继承：
外部动作与风险：
```

事实、素材和数据缺失写“缺”；当前阶段可以专业补全的创意不能只写“缺”，要提出并选择方案。会改变核心故事、真实声称、成本、版权、安全和公开发布的内容才需要确认。

## 五、生产状态

```text
IDEA → STORY → ASSET → SHOT → PROMPT → GENERATED → EDITED → PUBLISHED → LEARNED
```

状态只在完成条件真实满足后推进：

| 状态 | 完成条件 |
|---|---|
| IDEA | 目标与原始材料已归档 |
| STORY | 观众等待、人物选择、因果、场次结果与下游事实成立 |
| ASSET | 最低可行资产已定义；需要实际图片时已标明待生成或已验收 |
| SHOT | 镜头任务、VID 边界、相邻状态与剪辑意图明确 |
| PROMPT | 三段式、资产引用、时空因果、字符与格式通过生成前验收 |
| GENERATED | 有真实生成文件或可靠回传，不是只有提示词 |
| EDITED | 有可观看的剪辑结果与真实时间码 |
| PUBLISHED | 有真实发布记录；数据尚未回收可标待回收 |
| LEARNED | 经验已写回唯一负责人，并通过代表性行为验证 |

快速草稿可以跨过部分正式文件，但不能伪造状态。只有提示词时不能写 GENERATED，只有发布计划时不能写 PUBLISHED。

## 六、相邻单元交接合同

两个节拍、场次、镜头、VID 或上下集只要共享人物、道具、空间、时间、动作、声音、情绪余势或永久结果，就逐对维护：

```text
单元 A / 单元 B：
关系：直接连续 / 同场重建 / 关系重置。
A 已完成结果：
A 结束状态：人物 / 服装伤势 / 道具 / 空间时间 / 永久结果 / 动作表演 / 声场。
B 起点状态（即开始状态）：
必须不变 / 允许变化：
主匹配属性 / 辅助匹配属性：
B 第一项新动作或声音：
下一相位：
参考路线：稳定资产 / 终帧 / 视频尾部 / 干净构图 / 无。
干净把手 / 禁止状态：
状态等式：通过 / 不通过。
不通过时返回的最早负责人：
```

直接连续执行 `A 结束状态 = B 起点状态`，但 B 不复演 A。计划状态只供设计；A 实际生成后由复盘 Skill 用真实结果二次绑定。

## 七、阶段交接

### 剧本 → 资产 / 视频

```text
观众等待与主情绪：
人物目标、阻力、选择和代价：
观众发现顺序：
场次结果与下一场压力：
已确认人物、道具、空间和时间事实：
暂时无法解决的具体状态：
相邻单元：
需要稳定的对象：
允许下游设计的观看方式：
```

### 资产 → 视频 / 封面

```text
最低可行资产：
已确认图片 / 待生成占位：
规范词账本：
依赖顺序：
每个参考只负责什么：
禁止继承什么：
阶段状态怎样替换：
没有资产、必须由视频正文写足的对象：
允许镜头重新设计的部分：
```

### 视频 → VC / 人工生成 / 复盘

```text
VID、确定时长与模型：
三段式正文：
实际绑定资产：
起点、触发、屏幕变化、结束状态：
相邻单元状态：
素材剪辑接口卡：
不可删不变量：
允许 VC 增强的部分：
字符实测：
真实回传要求：
```

### VC → 视频

```text
执行动作：放行 / 提纯 / 改写 / 可选版 / 保留 / 退回。
唯一增益目标：
实际增强的可见 / 可听结果：
保留的硬接口：
与原稿对比结论：
改动后字符实测：
```

### 封面 → 人工生图 / 复盘

```text
点击后真正能看到的内容：
主承诺与第一落点：
标题权限与逐字原文：
实际参考及职责：
比例自适应提示词：
生成后逐字核对与缩略图检查：
```

### 复盘 → 上游 / 总控

```text
证据完整度：
第一次观看与第一个断点：
最早负责人：
需要保护的成功点：
最多三个返修动作：
真实素材入点 / 出点 / 把手：
规则回流位置：
KEEP / OBSERVE / REVERT：
```

## 八、Reference 合约

Reference 只承接特定题材、对象、模型、证据或失败分支。Skill 主体必须写清触发信号、读取文件和停止条件。

```text
完全适配：一个 Reference 覆盖主导机制。
组合适配：一个主要 Reference，最多一个辅助 Reference。
能力缺口：建立当前作品机制卡；有复用证据后再增加长期能力。
```

只改变颜色、年代、人物和表面风格，不自动构成新能力。新 Reference 以稳定观众任务和主导机制命名，不以单个案例、软件、人物、颜色和特效命名。

## 九、失败返回

| 发现问题的阶段 | 返回条件 |
|---|---|
| 资产 | 人物动机、故事结果或信息顺序不成立 → 编剧 |
| 视频 | 镜头尚未发明 → 分镜 / 编剧；身份或空间无稳定基准 → 资产 |
| VC | 三段、字符、连续性和动作基线不合格 → 视频；故事与资产缺口返回对应负责人 |
| 封面 | 正片没有可兑现承诺 → 编剧 / 视频；缺干净身份参考 → 资产 |
| 复盘 | 依据第一个断点返回最早负责人，不默认归因模型 |

返回必须带证据、保护项和完成条件，不能只写“上游有问题”。

## 十、共同验收

- 当前是否只有一个主负责人。
- 阶段入口、完成条件和下游是否清楚。
- 已确认事实是否被保护，新增创意是否在授权范围。
- 相邻单元是否状态相等并从下一相位开始。
- Reference 是否按机制触发，不是按题材名全读。
- 结果是否能直接进入下一阶段，不依赖作者口头补充。
- 路径、字符、格式、命名和平台边界是否经过机械验证。
- 反馈是否回到唯一规则负责人，并有行为验证。
